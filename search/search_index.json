{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Patternia Documentation","text":"<p>A modern C++ pattern matching DSL</p> <p>Patternia is a modern C++ pattern-matching DSL designed to make dispatch logic clear, expressive, and type-safe - without sacrificing performance or compilation efficiency.</p>"},{"location":"#navigation","title":"Navigation","text":""},{"location":"#start-here","title":"Start Here","text":"<ul> <li>Getting Started - Minimal examples and core syntax</li> <li>Installation - How to integrate Patternia into your project</li> </ul>"},{"location":"#concepts-and-tutorials","title":"Concepts and Tutorials","text":"<ul> <li>From Control Flow to Pattern Matching - Onramp for new users</li> <li>Pattern Matching in Other Languages - Mapping familiar ideas to Patternia</li> <li>Custom Predicate Guards - Guard design and composition</li> </ul>"},{"location":"#worked-examples","title":"Worked Examples","text":"<ul> <li>Policy Constraint Matching - Declarative rules and access control</li> <li>Geometric Constraint Matching - Numeric constraints as patterns</li> </ul>"},{"location":"#reference","title":"Reference","text":"<ul> <li>API Documentation - Complete API reference for all patterns and functions</li> <li>Performance Notes - Algorithm evolution and dispatch strategy by version</li> <li>Design Overview - Semantics and matching model</li> </ul>"},{"location":"#project-information","title":"Project Information","text":"<ul> <li>Releases - Version update history</li> <li>Benchmark Scripts - JSON compare and visualization tooling</li> </ul>"},{"location":"#system-requirements","title":"System Requirements","text":"<ul> <li>C++ Standard: C++17 or later</li> <li>Compilers: GCC &gt;= 11, Clang &gt;= 12, MSVC &gt;= 2019</li> <li>Platforms: Linux, Windows, macOS</li> </ul>"},{"location":"#benchmarking","title":"Benchmarking","text":"<ul> <li>Run benchmark suite from project root:</li> </ul> <pre><code>.\\build\\bench\\ptn_bench.exe --benchmark_filter=\"Variant\" --benchmark_out=build/variant_all.json --benchmark_out_format=json\n</code></pre> <ul> <li>Visualize one JSON across implementations:</li> </ul> <pre><code>py -3 scripts/bench_single_report.py --input build/variant_all.json --include \"Variant\" --outdir build/bench/single --prefix variant_single\n</code></pre> Make logical structure explicit, make C++ code more elegant"},{"location":"api/","title":"Patternia API Reference","text":""},{"location":"api/#overview","title":"Overview","text":"<p>Patternia exposes one public matching shape:</p> <pre><code>match(subject) | on(\n  case_1,\n  case_2,\n  _ &gt;&gt; fallback\n)\n</code></pre> <p>The pipeline is immediate. There is no deferred builder stage.</p>"},{"location":"api/#matchsubject","title":"<code>match(subject)</code>","text":"<p><code>match(subject)</code> creates the evaluation context for one subject value.</p> <pre><code>int x = 7;\nauto r = match(x) | on(\n  lit(7) &gt;&gt; 1,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>Rules:</p> <ul> <li><code>subject</code> must be an lvalue.</li> <li>Cases are evaluated in source order.</li> <li>Matching uses first-match-wins semantics.</li> </ul>"},{"location":"api/#oncases","title":"<code>on(case1, case2, ...)</code>","text":"<p><code>on(...)</code> groups the case list consumed by <code>match(subject)</code>.</p> <pre><code>match(x) | on(\n  lit(1) &gt;&gt; \"one\",\n  lit(2) &gt;&gt; \"two\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Rules:</p> <ul> <li>The last case must be a wildcard fallback.</li> <li>Each entry must be a <code>pattern &gt;&gt; handler</code> case expression.</li> <li>Unreachable wildcard/alt ordering errors are diagnosed at compile time.</li> </ul>"},{"location":"api/#pattern-handler","title":"<code>pattern &gt;&gt; handler</code>","text":"<p>This is the core case-expression form.</p> <pre><code>lit(1) &gt;&gt; 42\n$(is&lt;int&gt;) &gt;&gt; [](int v) { return v * 2; }\n$(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) { return x + y; }\n</code></pre> <p>Handler forms:</p> <ul> <li><code>pattern &gt;&gt; value</code></li> <li><code>pattern &gt;&gt; callable</code></li> </ul> <p>Bindings are determined entirely by the pattern.</p>"},{"location":"api/#_-wildcard","title":"Wildcard <code>_</code>","text":"<p><code>_</code> is the public wildcard and fallback pattern.</p> <pre><code>match(x) | on(\n  lit(1) &gt;&gt; \"one\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>The wildcard does not bind values by itself.</p>"},{"location":"api/#literal-patterns","title":"Literal Patterns","text":""},{"location":"api/#litvalue","title":"<code>lit(value)</code>","text":"<p>Runtime literal match.</p> <pre><code>match(x) | on(\n  lit(5) &gt;&gt; 42,\n  _ &gt;&gt; -1\n);\n</code></pre>"},{"location":"api/#valvalue","title":"<code>val&lt;value&gt;</code>","text":"<p>Compile-time literal match. Useful when the literal is known at compile time and the lowering engine can consider static dispatch.</p> <pre><code>match(x) | on(\n  val&lt;1&gt; &gt;&gt; 1,\n  val&lt;2&gt; &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"api/#lit_civalue","title":"<code>lit_ci(value)</code>","text":"<p>Runtime ASCII case-insensitive string match.</p> <pre><code>match(s) | on(\n  lit_ci(\"hello\") &gt;&gt; 1,\n  _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"api/#binding-patterns","title":"Binding Patterns","text":""},{"location":"api/#and","title":"<code>$</code> and <code>$(...)</code>","text":"<p>Patternia keeps binding explicit.</p> <pre><code>$               // bind the whole subject\n$(subpattern)   // bind under a subpattern\n</code></pre> <p>Examples:</p> <pre><code>match(x) | on(\n  $ &gt;&gt; [](int v) { return v; },\n  _ &gt;&gt; 0\n);\n</code></pre> <pre><code>match(v) | on(\n  $(is&lt;std::string&gt;) &gt;&gt; [](const std::string &amp;s) {\n    return s.size();\n  },\n  _ &gt;&gt; 0\n);\n</code></pre> <p>Binding rules:</p> <ul> <li>No pattern binds implicitly.</li> <li>Handler parameters follow binding order.</li> <li>Patterns that do not bind produce zero-argument handlers.</li> </ul>"},{"location":"api/#guard-attachment","title":"Guard Attachment <code>[]</code>","text":"<p>Attach a guard to a binding pattern with <code>pattern[guard]</code>.</p> <pre><code>match(x) | on(\n  $[PTN_LET(value, value &gt; 0 &amp;&amp; value &lt; 10)] &gt;&gt; \"small\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Guard evaluation order:</p> <ol> <li>Match the pattern.</li> <li>Bind values.</li> <li>Evaluate the guard.</li> <li>Invoke the handler if the guard passes.</li> </ol> <p>A guard failure only rejects the current case.</p>"},{"location":"api/#guard-helpers","title":"Guard Helpers","text":""},{"location":"api/#_0","title":"<code>_0</code>","text":"<p>Placeholder alias for a single bound value.</p> <pre><code>$[_0 &gt; 5]\n</code></pre> <p>Use <code>_0</code> when one binding is enough and the predicate reads clearly without an explicit name.</p>"},{"location":"api/#argn","title":"<code>arg&lt;N&gt;</code>","text":"<p>Indexed placeholder for general multi-binding guards.</p> <pre><code>$(has&lt;&amp;Point::x, &amp;Point::y&gt;)[arg&lt;0&gt; * arg&lt;0&gt; + arg&lt;1&gt; * arg&lt;1&gt; == 25]\n</code></pre>"},{"location":"api/#rnglo-hi-mode","title":"<code>rng(lo, hi, mode)</code>","text":"<p>Range helper for single-bound-value guards.</p> <pre><code>$[rng(0, 10)]\n$[rng(0, 10, pat::mod::open)]\n</code></pre> <p>Use callables for domain logic that does not read naturally as <code>_0</code>, <code>arg&lt;N&gt;</code>, or <code>PTN_WHERE(...)</code>.</p>"},{"location":"api/#ptn_wherenames-expr","title":"<code>PTN_WHERE((names...), expr)</code>","text":"<p>Use named guard parameters without writing the lambda yourself. The macro currently supports 1 to 5 names.</p> <pre><code>match(p) | on(\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;)[PTN_WHERE((x, y), x == y)] &gt;&gt; \"diagonal\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p><code>PTN_WHERE(...)</code> expands to a guard callable and composes with <code>&amp;&amp;</code> / <code>||</code> like other guard predicates.</p>"},{"location":"api/#ptn_letname-expr","title":"<code>PTN_LET(name, expr)</code>","text":"<p>Use the single-value form when a guard binds exactly one value and you want to  name it explicitly.</p> <pre><code>match(x) | on(\n  $[PTN_LET(value, value &gt; 0 &amp;&amp; value &lt; 10)] &gt;&gt; \"small\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p><code>PTN_LET(name, expr)</code> is equivalent to <code>PTN_WHERE((name), expr)</code>.</p>"},{"location":"api/#structural-matching","title":"Structural Matching <code>has&lt;&amp;T::member...&gt;</code>","text":"<p><code>has&lt;&gt;</code> describes structure. Wrap it with <code>$(...)</code> to extract values.</p> <pre><code>struct Point {\n  int x;\n  int y;\n};\n\nmatch(p) | on(\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) {\n    return x + y;\n  },\n  _ &gt;&gt; 0\n);\n</code></pre> <p>Properties:</p> <ul> <li>Member order is explicit and stable.</li> <li>Unlisted members are ignored.</li> <li>Validation happens at compile time.</li> </ul>"},{"location":"api/#variant-matching","title":"Variant Matching <code>is&lt;T&gt;</code> and <code>alt&lt;I&gt;</code>","text":""},{"location":"api/#ist","title":"<code>is&lt;T&gt;</code>","text":"<p>Type-based <code>std::variant</code> match.</p> <pre><code>match(v) | on(\n  is&lt;int&gt; &gt;&gt; \"int\",\n  $(is&lt;std::string&gt;) &gt;&gt; [](const std::string &amp;s) {\n    return \"str:\" + s;\n  },\n  _ &gt;&gt; [] { return std::string(\"other\"); }\n);\n</code></pre>"},{"location":"api/#alti","title":"<code>alt&lt;I&gt;</code>","text":"<p>Index-based <code>std::variant</code> match.</p> <pre><code>match(v) | on(\n  alt&lt;0&gt; &gt;&gt; \"first\",\n  alt&lt;1&gt; &gt;&gt; \"second\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Rules:</p> <ul> <li><code>is&lt;T&gt;</code> requires <code>T</code> to appear exactly once.</li> <li><code>alt&lt;I&gt;</code> requires <code>I</code> to be in range.</li> <li>Use <code>$(...)</code> when you want the alternative value bound into the handler.</li> </ul>"},{"location":"api/#pattern-combinators","title":"Pattern Combinators","text":""},{"location":"api/#anyps","title":"<code>any(ps...)</code>","text":"<p>Matches when at least one sub-pattern matches (OR semantics). Short-circuits on the first matching sub-pattern.</p> <pre><code>match(x) | on(\n  any(val&lt;1&gt;, val&lt;2&gt;, val&lt;3&gt;) &gt;&gt; \"1, 2, or 3\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Properties:</p> <ul> <li>Non-binding: handlers receive zero arguments.</li> <li>Sub-patterns are evaluated left-to-right; evaluation stops at the first   match.</li> <li>Requires at least one sub-pattern; every argument must be a pattern object.</li> </ul>"},{"location":"api/#allps","title":"<code>all(ps...)</code>","text":"<p>Matches only when every sub-pattern matches (AND semantics). Short-circuits on the first mismatch.</p> <pre><code>match(x) | on(\n  all(any(val&lt;1&gt;, val&lt;2&gt;), val&lt;2&gt;) &gt;&gt; \"2\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Properties:</p> <ul> <li>Non-binding: handlers receive zero arguments.</li> <li>Sub-patterns are evaluated left-to-right; evaluation stops at the first   mismatch.</li> <li>Requires at least one sub-pattern; every argument must be a pattern object.</li> </ul>"},{"location":"api/#cached-case-packs","title":"Cached Case Packs","text":""},{"location":"api/#static_on","title":"<code>static_on(...)</code>","text":"<p>Cache a stateless <code>on(...)</code> factory.</p> <pre><code>match(x) | static_on([] {\n  return on(\n    val&lt;1&gt; &gt;&gt; 1,\n    val&lt;2&gt; &gt;&gt; 2,\n    _ &gt;&gt; 0\n  );\n});\n</code></pre>"},{"location":"api/#ptn_on","title":"<code>PTN_ON(...)</code>","text":"<p>Convenience macro over <code>static_on(...)</code>.</p> <pre><code>match(x) | PTN_ON(\n  val&lt;1&gt; &gt;&gt; 1,\n  val&lt;2&gt; &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>The factory must be stateless.</p>"},{"location":"api/#namespace-summary","title":"Namespace Summary","text":"<p>The public surface is re-exported through <code>namespace ptn</code>:</p> <ul> <li><code>match</code></li> <li><code>on</code></li> <li><code>lit</code>, <code>val</code>, <code>lit_ci</code></li> <li><code>$</code></li> <li><code>_</code></li> <li><code>_0</code>, <code>arg</code>, <code>rng</code></li> <li><code>has</code></li> <li><code>is</code>, <code>alt</code></li> <li><code>any</code>, <code>all</code></li> </ul>"},{"location":"api/#minimal-example","title":"Minimal Example","text":"<pre><code>#include &lt;ptn/patternia.hpp&gt;\n\nint main() {\n  using namespace ptn;\n\n  int x = 2;\n  int r = match(x) | on(\n    lit(1) &gt;&gt; 10,\n    lit(2) &gt;&gt; 20,\n    _ &gt;&gt; 0\n  );\n\n  return r == 20 ? 0 : 1;\n}\n</code></pre>"},{"location":"design-overview/","title":"Design Overview","text":"<p>Patternia treats pattern matching as an expression over a concrete subject:</p> <pre><code>match(subject) | on(\n  case_1,\n  case_2,\n  _ &gt;&gt; fallback\n)\n</code></pre> <p>The library is built around four ideas:</p> <ol> <li>Subject flow is explicit.</li> <li>Binding is explicit.</li> <li>Cases are ordered and first-match-wins.</li> <li>The public syntax stays small.</li> </ol>"},{"location":"design-overview/#subject-first-matching","title":"Subject-First Matching","text":"<p>Patternia does not hide the matched value inside an implicit control-flow form. You always start with the subject:</p> <pre><code>match(x) | on(\n  lit(1) &gt;&gt; \"one\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>This makes the match expression read as data flow rather than as a block-level statement form.</p>"},{"location":"design-overview/#immediate-evaluation","title":"Immediate Evaluation","text":"<p><code>match(subject)</code> by itself is only the left side of the pipeline. Evaluation happens when the <code>on(...)</code> case pack is supplied:</p> <pre><code>auto result = match(x) | on(\n  lit(0) &gt;&gt; 0,\n  lit(1) &gt;&gt; 1,\n  _ &gt;&gt; -1\n);\n</code></pre> <p>There is no separate chained builder API.</p>"},{"location":"design-overview/#explicit-binding","title":"Explicit Binding","text":"<p>Matching and binding are deliberately separate. Patterns answer \"does this match?\" Binding answers \"what values are exposed to the handler?\"</p> <pre><code>match(x) | on(\n  $ &gt;&gt; [](int v) { return v; },\n  _ &gt;&gt; 0\n);\n</code></pre> <pre><code>match(p) | on(\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) {\n    return x + y;\n  },\n  _ &gt;&gt; 0\n);\n</code></pre> <p>This separation keeps handler signatures predictable and keeps data flow local.</p>"},{"location":"design-overview/#guards-refine-a-bound-case","title":"Guards Refine a Bound Case","text":"<p>Guards apply after a binding pattern succeeds.</p> <pre><code>match(x) | on(\n  $[PTN_LET(value, value &gt; 0 &amp;&amp; value &lt; 10)] &gt;&gt; \"small\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>The evaluation model is:</p> <ol> <li>Match the pattern.</li> <li>Bind values.</li> <li>Evaluate the guard.</li> <li>Run the handler on success.</li> </ol> <p>If the guard fails, matching continues with the next case.</p>"},{"location":"design-overview/#wildcard-fallback","title":"Wildcard Fallback","text":"<p>Patternia requires an explicit fallback case in <code>on(...)</code>.</p> <pre><code>match(v) | on(\n  is&lt;int&gt; &gt;&gt; \"int\",\n  is&lt;std::string&gt; &gt;&gt; \"string\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>This keeps the terminal behavior visible inside the case list itself.</p>"},{"location":"design-overview/#structural-and-variant-matching-share-one-core","title":"Structural and Variant Matching Share One Core","text":"<p>Literal matching, structural matching, guarded binding, and variant dispatch all lower through the same ordered case model.</p> <pre><code>match(value) | on(\n  lit(1) &gt;&gt; \"literal\",\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) {\n    return x + y &gt; 0 ? \"point\" : \"origin-side\";\n  },\n  $(is&lt;std::string&gt;)[PTN_LET(text, text != \"\")] &gt;&gt; \"string\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>That shared model is what keeps the DSL small without giving up expressive power.</p>"},{"location":"design-overview/#performance-direction","title":"Performance Direction","text":"<p>Patternia aims at zero-overhead abstractions:</p> <ul> <li>no virtual dispatch</li> <li>no RTTI</li> <li>no heap allocation</li> <li>aggressive inlining and compile-time validation</li> </ul> <p>For repeated hot paths, users can cache case packs with <code>static_on(...)</code> or <code>PTN_ON(...)</code>.</p>"},{"location":"design-overview/#summary","title":"Summary","text":"<p>Patternia is intentionally narrow:</p> <ul> <li>one subject-first entry</li> <li>one case-pack terminal form</li> <li>explicit binding</li> <li>explicit wildcard fallback</li> </ul> <p>That constraint is part of the design.</p>"},{"location":"roadmap/","title":"Roadmap","text":"<p>This page tracks API direction and current implementation status.</p> <p>Status labels: - <code>FINISHED</code>: already shipped and documented. - <code>WIP</code>: currently being designed or prepared. - <code>NEXT</code>: future consideration, not yet committed.</p>"},{"location":"roadmap/#finished","title":"FINISHED","text":"<p>Completed in v0.9.2:</p> <ul> <li><code>any(ps...)</code>, <code>all(ps...)</code> pattern combinators with OR/AND semantics.</li> </ul> <p>Completed in v0.9.1:</p> <ul> <li><code>val&lt;V&gt;</code> static literal entry point.</li> <li>C++20 floating-point static literals through <code>val&lt;V&gt;</code>.</li> <li>Canonical structural entry <code>has&lt;...&gt;</code> (factory-style <code>has&lt;...&gt;()</code> removed   from public API).</li> </ul>"},{"location":"roadmap/#wip","title":"WIP","text":"<p>Current workstream for upcoming releases.</p>"},{"location":"roadmap/#negp-negation-pattern","title":"<code>neg(p)</code> \u2014 negation pattern","text":"<p>Invert the result of a sub-pattern.</p> <pre><code>match(x) | on(\n  neg(val&lt;0&gt;) &gt;&gt; \"non-zero\",\n  _ &gt;&gt; \"zero\"\n);\n</code></pre>"},{"location":"roadmap/#predcallable-predicate-pattern","title":"<code>pred(callable)</code> \u2014 predicate pattern","text":"<p>Lift an arbitrary unary predicate into a first-class pattern, evaluated during the match phase rather than as a post-bind guard.</p> <pre><code>match(x) | on(\n  pred([](int v) { return v % 2 == 0; }) &gt;&gt; \"even\",\n  _ &gt;&gt; \"odd\"\n);\n</code></pre>"},{"location":"roadmap/#next","title":"NEXT","text":"<p>Potential follow-up items after current WIP scope is stabilized.</p>"},{"location":"roadmap/#some-none-stdoptional-patterns","title":"<code>some</code> / <code>none</code> \u2014 <code>std::optional</code> patterns","text":"<p>Dedicated patterns for the two states of <code>std::optional</code>.</p> <pre><code>std::optional&lt;int&gt; v = 42;\nmatch(v) | on(\n  $(some) &gt;&gt; [](int x) { return x; },\n  none &gt;&gt; 0\n);\n</code></pre>"},{"location":"roadmap/#design-principles-for-new-api","title":"Design Principles for New API","text":"<ul> <li>Stateless compile-time patterns should be variable templates, not   function templates that return default-constructed objects.</li> <li>Function template forms are reserved for patterns that require runtime   arguments (e.g., <code>lit(value)</code>, <code>lit_ci(value)</code>, <code>rng(lo, hi)</code>).</li> <li>Names should be short, lowercase, and read naturally in the DSL.</li> </ul>"},{"location":"changelog/releases/","title":"Patternia Releases","text":"<p>Historical release notes have been normalized where necessary so examples reflect the current supported API surface.</p>"},{"location":"changelog/releases/#05x","title":"0.5.x","text":"<ul> <li>v0.5.2 - December 10, 2025</li> <li>v0.5.3 - December 14, 2025</li> </ul>"},{"location":"changelog/releases/#06x","title":"0.6.x","text":"<ul> <li>v0.6.0 - December 16, 2025</li> <li>v0.6.1 - December 20, 2025</li> <li>v0.6.2 - December 23, 2025</li> <li>v0.6.3 - January 2, 2026</li> </ul>"},{"location":"changelog/releases/#07x","title":"0.7.x","text":"<ul> <li>v0.7.0 - February 5, 2026</li> <li>v0.7.1 - February 7, 2026</li> <li>v0.7.2 - February 14, 2026</li> <li>v0.7.3 - February 16, 2026</li> <li>v0.7.4 - February 18, 2026</li> <li>v0.7.5 - February 18, 2026 (Hotfix)</li> <li>v0.7.6 - February 21, 2026</li> </ul>"},{"location":"changelog/releases/#08x","title":"0.8.x","text":"<ul> <li>v0.8.0 - February 27, 2026</li> <li>v0.8.2 - March 3, 2026</li> <li>v0.8.3 - March 9, 2026</li> <li>v0.8.5 - March 10, 2026</li> </ul>"},{"location":"changelog/releases/#09x","title":"0.9.x","text":"<ul> <li>v0.9.2 - April 10, 2026</li> <li>v0.9.1 - March 18, 2026</li> <li>v0.9.0 - March 13, 2026</li> </ul>"},{"location":"changelog/v0.5.2/","title":"Patternia v0.5.2 Release Note","text":"<p>Release Date: December 10, 2025 Version: 0.5.2</p>"},{"location":"changelog/v0.5.2/#overview","title":"Overview","text":"<p>Patternia v0.5.2 introduced the wildcard pattern <code>_</code> and clarified the fallback rules that still define the library today.</p> <p>This historical page has been normalized to the current pipeline API. The original release note used the removed chained builder syntax.</p>"},{"location":"changelog/v0.5.2/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.5.2/#wildcard-fallback","title":"Wildcard Fallback","text":"<p><code>ptn::_</code> was introduced as the catch-all pattern for the final case in a match.</p> <pre><code>using namespace ptn;\n\nconst char *label(int value) {\n  return match(value) | on(\n    lit(0) &gt;&gt; \"zero\",\n    _ &gt;&gt; \"other\"\n  );\n}\n</code></pre>"},{"location":"changelog/v0.5.2/#first-match-semantics","title":"First-Match Semantics","text":"<ul> <li>Cases are evaluated in order.</li> <li>The first matching case wins.</li> <li><code>_</code> matches any subject and binds nothing.</li> </ul>"},{"location":"changelog/v0.5.2/#design-direction","title":"Design Direction","text":"<p>This release established a direction that remains valid after later API cleanup:</p> <ul> <li>Fallback behavior belongs in the case list.</li> <li>Matching stays expression-oriented.</li> <li>Wildcard usage should be explicit and visible.</li> </ul>"},{"location":"changelog/v0.5.2/#what-still-matters","title":"What Still Matters","text":"<ul> <li>Prefer a final <code>_</code> case when you need a total match.</li> <li>Keep the wildcard last so earlier cases stay reachable.</li> <li>Treat <code>_</code> as a pattern-level fallback, not as a binding form.</li> </ul>"},{"location":"changelog/v0.5.2/#historical-note","title":"Historical Note","text":"<p>The original v0.5.2 note documented the old chained builder flow. That surface has since been removed from the repository. Current code should use <code>match(subject) | on(...)</code> instead.</p>"},{"location":"changelog/v0.5.3/","title":"Patternia v0.5.3 Release Note","text":"<p>Release Date: December 14, 2025 Version: 0.5.3</p>"},{"location":"changelog/v0.5.3/#overview","title":"Overview","text":"<p>Patternia v0.5.3 expanded the pattern system with binding, guards, structural matching, and range predicates.</p> <p>This page has been normalized to the current API. The original release note used the removed chained builder syntax and the older single-value placeholder type.</p>"},{"location":"changelog/v0.5.3/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.5.3/#binding-patterns","title":"Binding Patterns","text":"<p><code>bind()</code> and <code>$()</code> let handlers receive matched values explicitly.</p> <pre><code>using namespace ptn;\n\nint identity(int value) {\n  return match(value) | on(\n    $() &gt;&gt; [](int v) { return v; },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"changelog/v0.5.3/#guards","title":"Guards","text":"<p>Guard predicates can refine a successful pattern match.</p> <pre><code>using namespace ptn;\n\nconst char *bucket(int value) {\n  return match(value) | on(\n    bind()[_0 &gt; 10 &amp;&amp; _0 &lt; 100] &gt;&gt; \"two-digit\",\n    bind()[_0 &gt;= 100] &gt;&gt; \"large\",\n    _ &gt;&gt; \"small\"\n  );\n}\n</code></pre>"},{"location":"changelog/v0.5.3/#structural-matching","title":"Structural Matching","text":"<p><code>has&lt;&gt;</code> made structural decomposition part of the public DSL.</p> <pre><code>using namespace ptn;\n\nstruct Point {\n  int x;\n  int y;\n};\n\nint magnitude2(const Point &amp;point) {\n  return match(point) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;()) &gt;&gt; [](int x, int y) {\n      return x * x + y * y;\n    },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"changelog/v0.5.3/#range-predicates","title":"Range Predicates","text":"<p>Range helpers became part of the guard vocabulary for validation-oriented matches.</p> <pre><code>using namespace ptn;\n\nconst char *classify(int value) {\n  return match(value) | on(\n    bind()[rng(0, 10)] &gt;&gt; \"small\",\n    bind()[rng(10, 20, open)] &gt;&gt; \"medium\",\n    _ &gt;&gt; \"other\"\n  );\n}\n</code></pre>"},{"location":"changelog/v0.5.3/#what-still-matters","title":"What Still Matters","text":"<ul> <li>Binding is explicit: use <code>$()</code> or <code>bind()</code> when the handler needs data.</li> <li>Guards refine a pattern hit; they do not replace the pattern itself.</li> <li><code>has&lt;&gt;</code> and <code>rng(...)</code> remain current DSL components.</li> </ul>"},{"location":"changelog/v0.5.3/#historical-note","title":"Historical Note","text":"<p>The original v0.5.3 note referenced the earlier single-value guard placeholder form. Current code should use <code>_0</code> for single-value guards and <code>arg&lt;N&gt;</code> for multi-value guards.</p>"},{"location":"changelog/v0.6.0/","title":"Patternia v0.6.0 Release Note","text":"<p>Release Date: December 16, 2025 Version: 0.6.0</p>"},{"location":"changelog/v0.6.0/#overview","title":"Overview","text":"<p>Patternia v0.6.0 expanded the expressive range of the matcher with generic handlers, multi-value guards, and direct structural binding.</p> <p>This historical page has been normalized to the current pipeline API. The original release note used the removed chained builder syntax.</p>"},{"location":"changelog/v0.6.0/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.6.0/#generic-handlers","title":"Generic Handlers","text":"<p>Handlers can stay generic while still receiving correctly bound values.</p> <pre><code>using namespace ptn;\n\nstruct Matrix {\n  int rows;\n  int cols;\n  std::vector&lt;double&gt; data;\n};\n\nstd::string describe(const Matrix &amp;matrix) {\n  return match(matrix) | on(\n    $(has&lt;&amp;Matrix::rows, &amp;Matrix::cols, &amp;Matrix::data&gt;()) &gt;&gt;\n        [](auto rows, auto cols, const auto &amp;data) {\n          return std::to_string(rows) + \"x\" + std::to_string(cols) +\n                 \":\" + std::to_string(data.size());\n        },\n    _ &gt;&gt; [] { return std::string(\"invalid\"); }\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.0/#multi-value-guards","title":"Multi-Value Guards","text":"<p><code>arg&lt;N&gt;</code> made it possible to express relationships between multiple bound values.</p> <pre><code>using namespace ptn;\n\nstruct Rect {\n  int width;\n  int height;\n};\n\nconst char *classify(const Rect &amp;rect) {\n  return match(rect) | on(\n    bind(has&lt;&amp;Rect::width, &amp;Rect::height&gt;())\n        [arg&lt;0&gt; == arg&lt;1&gt;] &gt;&gt; \"square\",\n    bind(has&lt;&amp;Rect::width, &amp;Rect::height&gt;())\n        [arg&lt;0&gt; &gt; arg&lt;1&gt;] &gt;&gt; \"wide\",\n    _ &gt;&gt; \"other\"\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.0/#structural-binding","title":"Structural Binding","text":"<p>Binding a structural pattern forwards the selected members directly to the handler.</p> <pre><code>using namespace ptn;\n\nstruct Point {\n  int x;\n  int y;\n};\n\nint sum(const Point &amp;point) {\n  return match(point) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;()) &gt;&gt; [](int x, int y) {\n      return x + y;\n    },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.0/#what-still-matters","title":"What Still Matters","text":"<ul> <li>Generic lambdas work with Patternia's normal binding rules.</li> <li><code>arg&lt;N&gt;</code> is the current way to write relationships across bound values.</li> <li>Binding a structural pattern is still the direct route to member extraction.</li> </ul>"},{"location":"changelog/v0.6.0/#historical-note","title":"Historical Note","text":"<p>The original v0.6.0 note documented the same capabilities through the old builder-style entry points. Those have since been removed; use <code>match(subject) | on(...)</code> in current code.</p>"},{"location":"changelog/v0.6.1/","title":"Patternia v0.6.1 Release Note","text":"<p>Release Date: December 20, 2025 Version: 0.6.1</p>"},{"location":"changelog/v0.6.1/#overview","title":"Overview","text":"<p>Patternia v0.6.1 focused on evaluation semantics, forwarding behavior, and API clarity.</p> <p>This page has been normalized to the current API surface. The original release note described the removed chained termination flow and the removed compact case-pack form.</p>"},{"location":"changelog/v0.6.1/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.6.1/#value-category-preservation","title":"Value Category Preservation","text":"<p><code>match()</code> continued to preserve the subject category needed for binding and handler invocation.</p> <pre><code>using namespace ptn;\n\nint classify(std::vector&lt;int&gt; &amp;&amp;values) {\n  return match(std::move(values)) | on(\n    $() &gt;&gt; [](std::vector&lt;int&gt; &amp;&amp;xs) {\n      return xs.empty() ? 0 : 1;\n    },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.1/#fallback-discipline","title":"Fallback Discipline","text":"<p>This release period clarified a rule that still applies after the builder cleanup: total matches should make the fallback explicit in the case list.</p> <pre><code>using namespace ptn;\n\nconst char *day_name(int day) {\n  return match(day) | on(\n    lit(1) &gt;&gt; \"Monday\",\n    lit(2) &gt;&gt; \"Tuesday\",\n    _ &gt;&gt; \"invalid\"\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.1/#diagnostics-direction","title":"Diagnostics Direction","text":"<p>The project kept moving toward earlier validation and clearer compile-time feedback for malformed matches.</p>"},{"location":"changelog/v0.6.1/#what-still-matters","title":"What Still Matters","text":"<ul> <li><code>match(subject)</code> remains the single public entry point.</li> <li>A final <code>_</code> case is the current total-match form.</li> <li>Diagnostics should steer users toward a single obvious spelling.</li> </ul>"},{"location":"changelog/v0.6.1/#historical-note","title":"Historical Note","text":"<p>The original v0.6.1 page covered removed termination helpers and the removed compact direct-entry form. Those APIs have been removed from the current repository. The underlying semantics now appear only through pipeline matching.</p>"},{"location":"changelog/v0.6.2/","title":"Patternia v0.6.2 Release Note","text":"<p>Release Date: December 23, 2025 Version: 0.6.2</p>"},{"location":"changelog/v0.6.2/#overview","title":"Overview","text":"<p>Patternia v0.6.2 tightened compile-time feedback for incomplete matches.</p> <p>This page has been normalized to the current API surface. The original release note described <code>[[nodiscard]]</code> on the old builder object; the same idea now applies to the pipeline match context.</p>"},{"location":"changelog/v0.6.2/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.6.2/#incomplete-match-diagnostics","title":"Incomplete Match Diagnostics","text":"<p>Ignoring a partially formed match is diagnosed so unfinished expressions do not silently disappear.</p> <pre><code>using namespace ptn;\n\nvoid run(int value) {\n  match(value);\n  // warning: incomplete match expression\n}\n</code></pre> <p>The intended form is to finish the match by piping an <code>on(...)</code> case pack:</p> <pre><code>using namespace ptn;\n\nint classify(int value) {\n  return match(value) | on(\n    lit(1) &gt;&gt; 1,\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.2/#safety-direction","title":"Safety Direction","text":"<p>This release reinforced a project rule that remains active:</p> <ul> <li>an unfinished match expression should be noisy</li> <li>diagnostics should tell users the next valid step</li> <li>API misuse should fail at compile time, not at runtime</li> </ul>"},{"location":"changelog/v0.6.2/#historical-note","title":"Historical Note","text":"<p>The original v0.6.2 note explained the warning in terms of a chained builder that was later removed. Current code still gets the same safety signal through the pipeline context returned by <code>match(subject)</code>.</p>"},{"location":"changelog/v0.6.3/","title":"Patternia v0.6.3 Release Note","text":"<p>Release Date: January 2, 2026 Version: 0.6.3</p>"},{"location":"changelog/v0.6.3/#overview","title":"Overview","text":"<p>Patternia v0.6.3 was an intermediate cleanup release on the path away from the old builder-heavy API.</p> <p>This page has been normalized to the current API surface. The original release note focused on left-value chained builder mutation, which is no longer part of the repository.</p>"},{"location":"changelog/v0.6.3/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.6.3/#fewer-staging-forms","title":"Fewer Staging Forms","text":"<p>The design direction in this release was to reduce partially mutated builder states and push users toward one-shot case assembly.</p> <p>That direction is now complete in the current API:</p> <pre><code>using namespace ptn;\n\nint classify(int value) {\n  return match(value) | on(\n    lit(1) &gt;&gt; \"one\",\n    lit(2) &gt;&gt; \"two\",\n    _ &gt;&gt; \"other\"\n  );\n}\n</code></pre>"},{"location":"changelog/v0.6.3/#simpler-evaluation-flow","title":"Simpler Evaluation Flow","text":"<p>By moving away from incremental builder mutation, the implementation can keep case ordering, fallback rules, and diagnostics localized to one explicit case pack.</p>"},{"location":"changelog/v0.6.3/#why-this-note-still-exists","title":"Why This Note Still Exists","text":"<p>v0.6.3 matters historically because it shows the library already moving away from mutable chained state. Later cleanups removed that builder surface entirely.</p>"},{"location":"changelog/v0.6.3/#historical-note","title":"Historical Note","text":"<p>The original v0.6.3 page documented staged builder mutation and related termination helpers. Those APIs have since been removed in favor of the single pipeline form <code>match(subject) | on(...)</code>.</p>"},{"location":"changelog/v0.7.0/","title":"Patternia v0.7.0 Release Note","text":"<p>Release Date: February 5, 2026 Version: 0.7.0</p>"},{"location":"changelog/v0.7.0/#overview","title":"Overview","text":"<p>Patternia v0.7.0 focuses on variant matching and diagnostics. This release unifies the type-pattern implementation (<code>is</code> / <code>$</code> / <code>alt</code>), improves variant alternative lookup, and tightens compile-time validation with clearer error messages. Documentation is updated to reflect the refined variant API and binding/guard semantics.</p>"},{"location":"changelog/v0.7.0/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.7.0/#variant-matching","title":"Variant Matching","text":"<ul> <li>Unified type-pattern implementation around <code>is&lt;T&gt;()</code>, <code>alt&lt;I&gt;()</code>, and   explicit binding via <code>$(is&lt;T&gt;())</code>.</li> <li>Added <code>alt&lt;I&gt;()</code> for index-based variant matching.</li> <li>Improved variant alternative lookup using a meta type list.</li> <li>Explicit binding of the selected alternative is expressed through <code>$()</code>   rather than a separate <code>as&lt;T&gt;()</code> surface.</li> </ul>"},{"location":"changelog/v0.7.0/#guards-and-bindings","title":"Guards and Bindings","text":"<ul> <li>Type patterns that bind can participate in guards.</li> <li>Binding/guard behavior and documentation clarified to keep semantics explicit.</li> </ul>"},{"location":"changelog/v0.7.0/#diagnostics-and-validation","title":"Diagnostics and Validation","text":"<ul> <li>Centralized and shortened static-assert diagnostics for clearer compiler output.</li> <li>Wildcard (<code>_</code>) unreachable-case detection and validation improved.</li> <li>Structural <code>has&lt;&gt;</code> members are validated at match-time for accessibility.</li> </ul>"},{"location":"changelog/v0.7.0/#api-changes","title":"API Changes","text":""},{"location":"changelog/v0.7.0/#legacy-compact-case-pack-restrictions","title":"Legacy Compact Case-Pack Restrictions","text":"<p>The removed compact case-pack form enforced non-binding patterns only, with explicit diagnostics. For binding and guard logic, use the pipeline form with explicit binding patterns.</p>"},{"location":"changelog/v0.7.0/#documentation-updates","title":"Documentation Updates","text":"<ul> <li>Variant matching documentation reorganized and expanded.</li> <li>Fallback semantics clarified around the final <code>_</code> case.</li> <li>Binding and guard behavior for variant alternatives documented more clearly.</li> <li>Navigation and link consistency fixes across docs.</li> </ul>"},{"location":"changelog/v0.7.0/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>If you relied on the removed compact case-pack form with bindings or guards,   migrate to the pipeline form.</li> <li>If you used duplicate types in <code>std::variant</code>, prefer <code>alt&lt;I&gt;()</code> for   unambiguous matching.</li> </ul>"},{"location":"changelog/v0.7.0/#implementation-notes-selected","title":"Implementation Notes (Selected)","text":"<ul> <li>Type-patterns now share a common internal skeleton for consistency.</li> <li>Static-assert messages are shorter and more uniform across modules.</li> <li>Variant alternative indexing now uses meta utilities (<code>index_t</code>) for stronger   compile-time reasoning.</li> </ul>"},{"location":"changelog/v0.7.0/#historical-note","title":"Historical Note","text":"<p>The original v0.7.0 note used earlier namespace-qualified variant pattern names. Current code should use <code>is&lt;T&gt;()</code>, <code>$(is&lt;T&gt;())</code>, and <code>alt&lt;I&gt;()</code>.</p>"},{"location":"changelog/v0.7.1/","title":"Patternia v0.7.1 Release Note","text":"<p>Release Date: February 7, 2026 Version: 0.7.1</p>"},{"location":"changelog/v0.7.1/#overview","title":"Overview","text":"<p>Patternia v0.7.1 is a diagnostics-focused patch release. This version improves compile-time error readability and adds direct fix hints for common misuse cases. No runtime behavior or matching semantics changed.</p>"},{"location":"changelog/v0.7.1/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.7.1/#clearer-static-assert-requirements","title":"Clearer Static-Assert Requirements","text":"<ul> <li>Replaced generic requirement names such as <code>ok</code> with semantic names   (for example <code>can_add_after_wildcard</code>, <code>alt_index_in_range</code>,   <code>pattern_has_bind_method</code>).</li> <li>Compiler diagnostics now expose more meaningful failed requirements.</li> </ul>"},{"location":"changelog/v0.7.1/#actionable-error-hints","title":"Actionable Error Hints","text":"<ul> <li>Added concise <code>Tip:</code> guidance to key diagnostics in pipeline validation.</li> <li>Improved guidance for wildcard placement, incomplete matches, and subject   category requirements.</li> </ul>"},{"location":"changelog/v0.7.1/#variant-type-diagnostics","title":"Variant Type Diagnostics","text":"<ul> <li>Improved <code>is&lt;T&gt;()</code> not-found diagnostics with clearer wording and   concrete recovery guidance.</li> <li>Added explicit guidance for duplicate variant alternatives:   prefer <code>alt&lt;I&gt;()</code> or distinct wrapper types.</li> </ul>"},{"location":"changelog/v0.7.1/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>Source compatibility: unchanged.</li> <li>API surface: unchanged.</li> <li>Behavior: unchanged (diagnostic wording only).</li> </ul>"},{"location":"changelog/v0.7.1/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>include/ptn/core/common/diagnostics.hpp</code></li> <li>Requirement names made semantic.</li> <li>Several static-assert messages now include fix-oriented hints.</li> <li><code>include/ptn/pattern/type.hpp</code></li> <li><code>is&lt;T&gt;()</code> alternative lookup diagnostics refined.</li> </ul>"},{"location":"changelog/v0.7.2/","title":"Patternia v0.7.2 Release Note","text":"<p>Release Date: February 14, 2026 Version: 0.7.2</p>"},{"location":"changelog/v0.7.2/#overview","title":"Overview","text":"<p>Patternia v0.7.2 is a diagnostics and correctness patch release focused on deterministic unreachable-case detection for <code>alt&lt;I&gt;()</code> case ordering. This release does not change runtime matching semantics.</p>"},{"location":"changelog/v0.7.2/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.7.2/#deterministic-unreachable-alti-diagnostics","title":"Deterministic Unreachable <code>alt&lt;I&gt;()</code> Diagnostics","text":"<ul> <li>Added compile-time detection for unreachable alt cases caused by ordering:   when a plain <code>alt&lt;I&gt;()</code> appears first, any later <code>alt&lt;I&gt;(...)</code>   case is now diagnosed as unreachable.</li> <li>Pipeline case diagnostics are scoped to the newly appended case to avoid   cascading duplicate errors on later checks.</li> <li>The removed compact case-pack form performed full-pack unreachable checks   consistently.</li> </ul>"},{"location":"changelog/v0.7.2/#diagnostics-infrastructure-improvements","title":"Diagnostics Infrastructure Improvements","text":"<ul> <li>Added internal alt-index extraction for both plain and guarded alt patterns.</li> <li>Unified unreachable-case reporting under a dedicated diagnostic path in   <code>diagnostics.hpp</code>.</li> </ul>"},{"location":"changelog/v0.7.2/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>Source compatibility: unchanged.</li> <li>API surface: unchanged.</li> <li>Behavior change: compile-time diagnostics are stricter for deterministic   unreachable <code>alt&lt;I&gt;()</code> ordering.</li> </ul>"},{"location":"changelog/v0.7.2/#implementation-notes-selected","title":"Implementation Notes (Selected)","text":"<ul> <li><code>include/ptn/core/common/diagnostics.hpp</code></li> <li>Added alt-index analysis traits and unreachable-after-plain-alt checker.</li> <li>Added targeted pipeline validation (<code>new case</code> only) and full-pack check.</li> <li><code>include/ptn/core/dsl/ops.hpp</code></li> <li>Legacy compact case-pack validation covered full alt-order reachability.</li> </ul>"},{"location":"changelog/v0.7.3/","title":"Patternia v0.7.3 Release Note\ud83c\udf86Happy Chinese NewYear!\ud83c\udf86","text":"<p>Release Date: February 16, 2026 Version: 0.7.3</p>"},{"location":"changelog/v0.7.3/#overview","title":"Overview","text":"<p>Patternia v0.7.3 is a performance-focused patch release. Compared with v0.7.2, this version reduces unnecessary value copies in binding paths and adds a repeatable heavy-bind benchmark workflow for regression tracking.</p>"},{"location":"changelog/v0.7.3/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.7.3/#binding-path-performance-optimization","title":"Binding Path Performance Optimization","text":"<ul> <li><code>bind()</code> now binds the subject by reference instead of materializing an   eager value copy.</li> <li><code>bind(subpattern)</code> now composes the subject binding by reference with   subpattern bindings.</li> <li>Structural binding (<code>bind(has&lt;...&gt;)</code>) now binds member fields by reference,   avoiding extra copies for large payload members.</li> </ul>"},{"location":"changelog/v0.7.3/#heavy-bind-benchmark-scenario","title":"Heavy-Bind Benchmark Scenario","text":"<ul> <li>Added <code>BM_Patternia_PacketMixedHeavyBind</code> to stress large payload binding   paths where copy/reference behavior is most visible.</li> <li>Added <code>BM_Switch_PacketMixedHeavyBind</code> as baseline for the same workload.</li> </ul>"},{"location":"changelog/v0.7.3/#benchmark-comparison-tooling","title":"Benchmark Comparison Tooling","text":"<ul> <li>Added <code>scripts/bench_stage_results.py</code> to normalize two <code>result.json</code> files   into a standard local layout.</li> <li>Added/updated <code>scripts/bench_compare.py</code> to render chart + markdown reports   with labels, filtering, and single-benchmark summary layout.</li> </ul>"},{"location":"changelog/v0.7.3/#performance-comparison-vs-v072","title":"Performance Comparison vs v0.7.2","text":"<p>For the heavy-bind scenario (<code>PacketMixedHeavyBind</code>), v0.7.3 is designed to highlight the effect of copy elimination in binding paths.</p> <p>Reference visualization generated from local compare workflow:</p> <p></p> <p>Note: absolute numbers vary by machine and run conditions. Use the staged compare scripts to evaluate your local baseline and current build under the same environment.</p>"},{"location":"changelog/v0.7.3/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>API surface: unchanged.</li> <li>Source compatibility: generally unchanged.</li> <li>Behavioral detail: bindings from <code>bind()</code>/structural bind paths are now   reference-oriented by default, reducing implicit copies.</li> </ul>"},{"location":"changelog/v0.7.3/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>include/ptn/pattern/bind.hpp</code></li> <li>Updated binding construction to reference-based tuples.</li> <li><code>bench/bench_match_variant.cpp</code></li> <li>Added heavy-bind packet workload and benchmark pairs.</li> <li><code>scripts/bench_stage_results.py</code></li> <li>Added benchmark result staging utility.</li> <li><code>scripts/bench_compare.py</code></li> <li>Added improved comparison rendering and filtering.</li> </ul>"},{"location":"changelog/v0.7.4/","title":"Patternia v0.7.4 Release Note","text":"<p>Release Date: February 18, 2026 Version: 0.7.4</p>"},{"location":"changelog/v0.7.4/#overview","title":"Overview","text":"<p>Patternia v0.7.4 is a follow-up performance and engineering patch release. Compared with v0.7.3, this version reduces extra work in guarded/typed match evaluation paths, expands regression coverage for binding behavior, and stabilizes variant benchmark and CI verification workflows.</p>"},{"location":"changelog/v0.7.4/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.7.4/#match-evaluation-performance-refinements","title":"Match Evaluation Performance Refinements","text":"<ul> <li>Guarded-case evaluation now avoids redundant binding work in the matched path.</li> <li>Typed-eval variant dispatch now takes a lower-overhead path for zero-bind   cases.</li> </ul>"},{"location":"changelog/v0.7.4/#test-coverage-expansion","title":"Test Coverage Expansion","text":"<ul> <li>Added bind-count regression tests for guarded match flow to validate that   matched guarded paths bind once (instead of legacy two-bind behavior).</li> <li>Added explicit guarded-path sequence checks to keep bind-order semantics   locked down.</li> </ul>"},{"location":"changelog/v0.7.4/#variant-focused-benchmarking","title":"Variant-Focused Benchmarking","text":"<ul> <li>Added variant-focused microbench coverage for dispatch-sensitive scenarios.</li> <li>Registered variant benchmark cases with a stable run profile so cross-run   comparisons are less noisy.</li> </ul>"},{"location":"changelog/v0.7.4/#ci-and-pipeline-improvements","title":"CI and Pipeline Improvements","text":"<ul> <li>Added benchmark gate workflow to track benchmark regressions in CI.</li> <li>Tightened default CI pipelines for faster and clearer quality feedback.</li> </ul>"},{"location":"changelog/v0.7.4/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>API surface: unchanged for 0.7.x users.</li> <li>Source compatibility: unchanged.</li> <li>Runtime behavior: same external semantics; internal evaluation paths are   optimized and validated with new regression tests.</li> </ul>"},{"location":"changelog/v0.7.4/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>include/ptn/core/common/eval.hpp</code></li> <li>Reduced redundant work in guarded/typed evaluation internals.</li> <li><code>tests/tests_match_flow.cpp</code></li> <li>Added guarded bind-count regression cases.</li> <li><code>bench/bench_match_variant.cpp</code></li> <li>Added variant-focused microbench case and stable benchmark profile.</li> <li><code>.github/workflows/ci.yml</code></li> <li>Updated CI flow and benchmark gate coverage.</li> </ul>"},{"location":"changelog/v0.7.5/","title":"Patternia v0.7.5 Hotfix Release Note","text":"<p>Release Date: February 18, 2026 Version: 0.7.5 Type: Hotfix</p>"},{"location":"changelog/v0.7.5/#overview","title":"Overview","text":"<p>Patternia v0.7.5 is a hotfix release for v0.7.4. It fixes a benchmark registration issue that can fail compilation on MinGW toolchains.</p>"},{"location":"changelog/v0.7.5/#what-is-fixed","title":"What Is Fixed","text":""},{"location":"changelog/v0.7.5/#benchmark-registration-compatibility-mingw","title":"Benchmark Registration Compatibility (MinGW)","text":"<ul> <li>Replaced <code>apply_stable_variant_profile(BENCHMARK(...))</code> registration style   with direct <code>BENCHMARK(...)-&gt;...</code> chained configuration.</li> <li>Preserved the same stable variant benchmark profile:   nanosecond unit, minimum run time, repetitions, and aggregate-only reporting.</li> </ul>"},{"location":"changelog/v0.7.5/#benchmark-warning-cleanup","title":"Benchmark Warning Cleanup","text":"<ul> <li>Updated the alternating-hot variant microbench path to avoid the deprecated   const-reference <code>DoNotOptimize</code> usage.</li> </ul>"},{"location":"changelog/v0.7.5/#impact","title":"Impact","text":"<ul> <li>API surface: unchanged.</li> <li>Runtime semantics: unchanged.</li> <li>Build behavior: benchmark target now compiles on MinGW where v0.7.4 could   fail.</li> </ul>"},{"location":"changelog/v0.7.5/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>bench/bench_match_variant.cpp</code></li> <li>Fixed benchmark macro registration pattern for compatibility.</li> <li>Kept benchmark profile semantics unchanged.</li> </ul>"},{"location":"changelog/v0.7.6/","title":"Patternia v0.7.6 Release Note","text":"<p>Release Date: February 21, 2026 Version: 0.7.6</p>"},{"location":"changelog/v0.7.6/#overview","title":"Overview","text":"<p>Patternia v0.7.6 is a performance and benchmarking release focused on variant dispatch internals, practical benchmark coverage, and easier benchmark result visualization.</p> <p>Compared with v0.7.5, this version adds targeted fast paths for variant matching (including mixed simple + guarded patterns), introduces real-world benchmark scenarios, and streamlines single-file benchmark reporting.</p>"},{"location":"changelog/v0.7.6/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.7.6/#variant-dispatch-performance-core","title":"Variant Dispatch Performance (Core)","text":"<ul> <li>Added a single-dispatch fast path for simple variant cases.</li> <li>Specialized simple dispatch by active variant index to reduce per-case   matching overhead.</li> <li>Added prefiltering for mixed simple + guarded variant patterns so guarded   branches can skip irrelevant alternatives early.</li> <li>Simplified internal index-resolution flow for typed variant dispatch.</li> </ul>"},{"location":"changelog/v0.7.6/#real-world-variant-benchmarks","title":"Real-World Variant Benchmarks","text":"<ul> <li>Added protocol-router scenario benchmarks.</li> <li>Added command-parser scenario benchmarks.</li> <li>For each scenario, included side-by-side implementations for:</li> <li>Patternia</li> <li>if-else</li> <li>switch-case</li> <li>std::visit</li> </ul>"},{"location":"changelog/v0.7.6/#benchmark-tooling-and-visualization","title":"Benchmark Tooling and Visualization","text":"<ul> <li>Added <code>scripts/bench_single_report.py</code> for single-JSON visualization across   multiple implementations in the same scenario.</li> <li>The script outputs chart + markdown + csv summaries to simplify local   analysis and sharing.</li> </ul>"},{"location":"changelog/v0.7.6/#documentation-refresh","title":"Documentation Refresh","text":"<ul> <li>Moved latest benchmark snapshot to the top of README for faster   performance-oriented access.</li> <li>Updated benchmark workflow docs with single-file visualization commands.</li> </ul>"},{"location":"changelog/v0.7.6/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>API surface: unchanged for 0.7.x users.</li> <li>Source compatibility: unchanged.</li> <li>Runtime semantics: unchanged externally; optimizations are internal to   variant evaluation and benchmark tooling.</li> </ul>"},{"location":"changelog/v0.7.6/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>include/ptn/core/common/eval.hpp</code></li> <li>Added and refined variant fast paths for simple and mixed-guarded cases.</li> <li><code>bench/bench_match_variant.cpp</code></li> <li>Added protocol-router and command-parser practical benchmark scenarios.</li> <li><code>scripts/bench_single_report.py</code></li> <li>Added single-JSON benchmark report generation utility.</li> <li><code>README.md</code></li> <li>Updated benchmark snapshot placement and workflow guidance.</li> <li><code>docs/index.md</code></li> <li>Synced benchmark tooling entry points for docs navigation.</li> </ul>"},{"location":"changelog/v0.8.0/","title":"Patternia v0.8.0 Release Note","text":"<p>Release Date: February 27, 2026 Version: 0.8.0</p>"},{"location":"changelog/v0.8.0/#overview","title":"Overview","text":"<p>Patternia v0.8.0 is an API and performance release.</p> <p>Compared with v0.7.6, this version removes the legacy compact terminal form and focuses the public usage on <code>match(x) | on(...)</code>. It also upgrades variant dispatch internals with a tiered hot/warm/cold strategy for better large-branch behavior.</p>"},{"location":"changelog/v0.8.0/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.8.0/#api-update-breaking","title":"API Update (Breaking)","text":"<ul> <li>Removed legacy terminal API:</li> <li>legacy compact terminal form</li> <li>Recommended forms in v0.8.x:</li> <li><code>match(x) | on(...);</code></li> </ul>"},{"location":"changelog/v0.8.0/#performance-core-dispatch-algorithms-in-v080","title":"Performance: Core Dispatch Algorithms in v0.8.0","text":"<ul> <li>Added explicit variant dispatch tiering:</li> <li><code>hot_inline</code> for small alternative counts</li> <li><code>warm_segmented</code> for medium alternative counts</li> <li><code>cold_compact</code> for large alternative counts</li> <li>Added compact index mapping for large typed-variant dispatch:</li> <li><code>active_index -&gt; compact slot</code></li> <li>miss goes directly to fallback path</li> <li>hit jumps into dense trampoline table</li> <li>Added typed-variant per-alt case-range narrowing:</li> <li>only scans the relevant case range for the active alternative</li> <li>Retained direct-reference bind fast path for variant typed bindings to reduce   tuple plumbing on common paths.</li> <li>Separated hot entry logic from cold large-branch logic using non-inline cold   helpers to reduce hot-path code footprint.</li> </ul>"},{"location":"changelog/v0.8.0/#internal-architecture-cleanup","title":"Internal Architecture Cleanup","text":"<ul> <li>Further separated optimization metadata from runtime evaluation flow:</li> <li>strategy/metadata stays in <code>optimize.hpp</code></li> <li>runtime execution stays in <code>eval.hpp</code></li> </ul>"},{"location":"changelog/v0.8.0/#migration-guide","title":"Migration Guide","text":"<p>If you still use the removed compact terminal API, migrate to pipeline syntax.</p> <p>After:</p> <pre><code>auto r = match(x) | on(\n  lit(1) &gt;&gt; [] { return 1; },\n  _ &gt;&gt; [] { return 0; }\n);\n</code></pre>"},{"location":"changelog/v0.8.0/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>API surface: breaking change in 0.8.0 due to removal of   the compact terminal API.</li> <li>Runtime semantics: unchanged for equivalent case ordering and fallback   behavior.</li> <li>Source compatibility: users of <code>match(x) | on(...)</code> remain compatible.</li> </ul>"},{"location":"changelog/v0.8.0/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>include/ptn/core/common/optimize.hpp</code></li> <li>Added dispatch tier model and variant metadata helpers.</li> <li><code>include/ptn/core/common/eval.hpp</code></li> <li>Integrated hot/warm/cold dispatch flow and cold-path helpers.</li> <li>Updated typed/simple variant dispatch to use compact metadata.</li> </ul>"},{"location":"changelog/v0.8.2/","title":"Patternia v0.8.2 Release Note","text":"<p>Release Date: March 3, 2026 Version: 0.8.2</p>"},{"location":"changelog/v0.8.2/#overview","title":"Overview","text":"<p>Patternia v0.8.2 is a performance and API refinement release.</p> <p>Compared with v0.8.0, this version introduces the lowering engine as the new optimization backbone, adds reusable pipeline matcher forms for hot paths, and formalizes the split between runtime literal factories and compile-time literal lowering.</p>"},{"location":"changelog/v0.8.2/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.8.2/#api-update","title":"API Update","text":"<ul> <li>Public examples now prefer:</li> <li><code>match(x) | on(...);</code></li> <li>Added reusable pipeline matcher forms:</li> <li><code>static_on(...)</code></li> <li><code>PTN_ON(...)</code></li> <li>Literal APIs are now explicitly split by purpose:</li> <li><code>lit(value)</code> for general runtime matching</li> <li><code>lit_ci(value)</code> for runtime case-insensitive string matching</li> <li><code>lit&lt;value&gt;()</code> for lowering-friendly compile-time literal dispatch</li> </ul>"},{"location":"changelog/v0.8.2/#performance-lowering-engine-in-v082","title":"Performance: Lowering Engine in v0.8.2","text":"<ul> <li>Introduced a shared lowering legality model:</li> <li><code>full</code></li> <li><code>bucketed</code></li> <li><code>none</code></li> <li>Added a switch-oriented static literal dispatch path for keyed matches that   fit the dense-span heuristics.</li> <li>Unified static literal and variant dispatch planning under the same   <code>dispatch_plan</code> model.</li> <li>Added reusable cached matcher forms so pipeline syntax can avoid repeated   matcher construction in hot loops.</li> </ul>"},{"location":"changelog/v0.8.2/#benchmark-snapshot","title":"Benchmark Snapshot","text":"<p>The release benchmark uses a 128-way static literal chain because it clearly separates two costs:</p> <ul> <li>dispatch quality</li> <li>matcher construction overhead</li> </ul> <p>Raw pipeline matcher vs <code>switch</code>:</p> <p></p> <p>Cached matcher forms vs <code>switch</code>:</p> <p></p> <p>Representative local results:</p> Benchmark Meaning CPU time <code>BM_PatterniaPipe_LiteralMatch128StaticCases</code> prebuilt matcher object <code>1.74 ns</code> <code>BM_PatterniaPipe_LiteralMatch128On</code> raw inline <code>on(...)</code> <code>17.9 ns</code> <code>BM_PatterniaPipe_LiteralMatch128OnMacro</code> cached pipeline matcher via <code>PTN_ON(...)</code> <code>2.09 ns</code> <code>BM_Switch_LiteralMatch128</code> hand-written <code>switch</code> baseline <code>1.52 ns</code>"},{"location":"changelog/v0.8.2/#migration-guide","title":"Migration Guide","text":"<p>This release does not require a forced source migration, but it changes the recommended style.</p> <p>Recommended in v0.8.2:</p> <pre><code>auto r = match(x) | on(\n  lit(1) &gt;&gt; 1,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>For hot paths, prefer cached matcher forms.</p> <p>Before:</p> <pre><code>auto r = match(x) | on(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  lit&lt;2&gt;() &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>After:</p> <pre><code>auto r = match(x) | PTN_ON(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  lit&lt;2&gt;() &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"changelog/v0.8.2/#compatibility-notes","title":"Compatibility Notes","text":"<ul> <li>API surface: additive in 0.8.2.</li> <li>Public examples now consistently use <code>on(...)</code>.</li> <li><code>lit(value)</code> and <code>lit_ci(value)</code> remain available for runtime matching.</li> <li><code>lit&lt;value&gt;()</code> is the new compile-time literal entry point for lowering.</li> <li>Runtime semantics remain first-match and fallback-compatible for equivalent   case ordering.</li> </ul>"},{"location":"changelog/v0.8.2/#implementation-notes","title":"Implementation Notes","text":"<ul> <li><code>include/ptn/core/common/optimize.hpp</code></li> <li>Added lowering legality analysis and shared <code>dispatch_plan</code> selection.</li> <li><code>include/ptn/core/common/eval.hpp</code></li> <li>Added switch-oriented static literal dispatch execution and updated     plan-driven evaluation.</li> <li><code>include/ptn/core/engine/match.hpp</code></li> <li>Added <code>static_on(...)</code> for cached pipeline matcher reuse.</li> <li><code>include/ptn/patternia.hpp</code></li> <li>Added <code>PTN_ON(...)</code> convenience sugar for cached pipeline syntax.</li> </ul> <p>For algorithm-level discussion, see:</p> <ul> <li>Performance Notes</li> </ul>"},{"location":"changelog/v0.8.3/","title":"Patternia v0.8.3 Release Note","text":"<p>Release Date: March 9, 2026 Version: 0.8.3</p>"},{"location":"changelog/v0.8.3/#overview","title":"Overview","text":"<p>Patternia v0.8.3 was an API ergonomics release in the 0.8 line. The direct <code>match(subject, case...)</code> entry introduced there has since been removed as the library converged on the pipeline form.</p>"},{"location":"changelog/v0.8.3/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.8.3/#historical-note-direct-match-entry","title":"Historical Note: Direct Match Entry","text":"<p>v0.8.3 temporarily added a direct <code>match(subject, case...)</code> overload. That form is no longer part of the public API. The modern spelling is:</p> <pre><code>int r = match(x) | on(\n    lit(1) &gt;&gt; 100,\n    lit(2) &gt;&gt; 200,\n    _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"changelog/v0.8.3/#wildcard-alias-_","title":"Wildcard Alias: <code>_</code>","text":"<p><code>_</code> is the public wildcard spelling:</p> <pre><code>match(x) | on(\n    lit(1) &gt;&gt; 10,\n    _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"changelog/v0.8.3/#bind-shorthand","title":"Bind Shorthand: <code>$</code>","text":"<p><code>$</code> is a concise alias for <code>bind()</code>, reducing noise in value-capture patterns:</p> <pre><code>match(x) | on(\n    $[_0 &gt; 0] &gt;&gt; [](int v) { return v * 2; },\n    _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"changelog/v0.8.3/#guard-placeholders-_0-_1-_2-_3","title":"Guard Placeholders: <code>_0</code>, <code>_1</code>, <code>_2</code>, <code>_3</code>","text":"<p>New shorthand aliases for <code>arg&lt;0&gt;</code> through <code>arg&lt;3&gt;</code>. The bare <code>_</code> guard placeholder is deprecated in favor of <code>_0</code>:</p> <pre><code>// Before\nbind()[_ &gt; 0 &amp;&amp; _ &lt; 10]\n\n// After\n$[_0 &gt; 0 &amp;&amp; _0 &lt; 10]\n</code></pre>"},{"location":"changelog/v0.8.3/#bug-fixes","title":"Bug Fixes","text":"<ul> <li>Fixed <code>pred_and</code> / <code>pred_or</code> failing to unwrap tuple when invoking raw   callables in mixed guard expressions (e.g., <code>_0 &gt; 1u &amp;&amp; is_even</code>). Added   <code>detail::invoke_guard</code> to handle tuple unpacking for non-guard predicates.</li> </ul>"},{"location":"changelog/v0.8.3/#deprecations","title":"Deprecations","text":"<ul> <li>The older single-value guard placeholder backing <code>_</code> is deprecated. Use <code>_0</code>   instead.   The <code>_</code> name is now reserved for the wildcard pattern.</li> </ul>"},{"location":"changelog/v0.8.3/#internal","title":"Internal","text":"<ul> <li>Migrated all tests, samples, and benchmarks from <code>_</code> to <code>_0</code> for guard   expressions.</li> <li>Updated documentation to reflect new placeholder naming.</li> </ul>"},{"location":"changelog/v0.8.5/","title":"Patternia v0.8.5 Release Note","text":"<p>Release Date: March 10, 2026 Version: 0.8.5</p>"},{"location":"changelog/v0.8.5/#overview","title":"Overview","text":"<p>Patternia v0.8.5 was an API modernization release focused on explicit binding, variant pattern naming, and guarded structural validation.</p> <p>This page has been normalized to the current API surface. The original release note mixed these changes with chained examples and older namespace-qualified variant names.</p>"},{"location":"changelog/v0.8.5/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.8.5/#variant-pattern-naming","title":"Variant Pattern Naming","text":"<p>Type-based variant matching uses the short public forms:</p> <ul> <li><code>is&lt;T&gt;()</code> for type-based dispatch</li> <li><code>alt&lt;I&gt;()</code> for index-based dispatch</li> <li><code>$(is&lt;T&gt;())</code> when the matched alternative must be bound</li> </ul> <pre><code>using namespace ptn;\n\nstd::string describe(const std::variant&lt;int, std::string&gt; &amp;value) {\n  return match(value) | on(\n    is&lt;int&gt;() &gt;&gt; \"int\",\n    $(is&lt;std::string&gt;()) &gt;&gt; [](const std::string &amp;text) {\n      return \"str:\" + text;\n    },\n    _ &gt;&gt; [] { return std::string(\"other\"); }\n  );\n}\n</code></pre>"},{"location":"changelog/v0.8.5/#callable-binding","title":"Callable <code>$()</code> Binding","text":"<p>The binding shorthand is uniform across literal, structural, and variant patterns.</p> <pre><code>using namespace ptn;\n\nstruct Point {\n  int x;\n  int y;\n};\n\nint sum(const Point &amp;point) {\n  return match(point) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;()) &gt;&gt; [](int x, int y) {\n      return x + y;\n    },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"changelog/v0.8.5/#structural-validation-without-binding","title":"Structural Validation Without Binding","text":"<p>Structural patterns can be guarded directly when a handler only needs a yes or no decision.</p> <pre><code>using namespace ptn;\n\nbool balanced(const Point &amp;point) {\n  return match(point) | on(\n    has&lt;&amp;Point::x, &amp;Point::y&gt;()[arg&lt;0&gt; + arg&lt;1&gt; == 0] &gt;&gt; true,\n    _ &gt;&gt; false\n  );\n}\n</code></pre>"},{"location":"changelog/v0.8.5/#migration-notes","title":"Migration Notes","text":"<ul> <li>Older namespace-qualified variant spellings have been retired in favor of   <code>is&lt;T&gt;()</code> and <code>alt&lt;I&gt;()</code>.</li> <li>Binding a selected variant alternative should use <code>$(is&lt;T&gt;())</code>.</li> <li>Binding structural members should use <code>$(has&lt;...&gt;())</code> or <code>bind(has&lt;...&gt;())</code>.</li> </ul>"},{"location":"changelog/v0.8.5/#historical-note","title":"Historical Note","text":"<p>The original v0.8.5 note introduced the same direction through a mixture of chained examples and older variant naming. Current code should use pipeline matching exclusively.</p>"},{"location":"changelog/v0.9.0/","title":"Patternia v0.9.0 Release Note","text":"<p>Release Date: March 13, 2026 Version: 0.9.0</p>"},{"location":"changelog/v0.9.0/#overview","title":"Overview","text":"<p>Patternia v0.9.0 is an API modernization and compiler compatibility release.</p> <p>It introduces <code>PTN_WHERE</code> and <code>PTN_LET</code> as the canonical named-guard syntax, removes the legacy <code>bind()</code> entry point and positional guard aliases <code>_1</code>/<code>_2</code>/<code>_3</code>, and ships a series of fixes that make callable guards work correctly across GCC, Clang, and MSVC. vcpkg packaging support is also added in this version.</p>"},{"location":"changelog/v0.9.0/#highlights","title":"Highlights","text":""},{"location":"changelog/v0.9.0/#named-guard-macros-ptn_where-and-ptn_let","title":"Named Guard Macros: <code>PTN_WHERE</code> and <code>PTN_LET</code>","text":"<p><code>PTN_WHERE((names...), expr)</code> gives guard arguments explicit names without writing a lambda. It supports 1 to 5 bound values:</p> <pre><code>using namespace ptn;\n\nstruct Point { int x; int y; };\n\nbool on_diagonal(const Point &amp;p) {\n  return match(p) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;())[PTN_WHERE((x, y), x == y)] &gt;&gt; true,\n    _ &gt;&gt; false\n  );\n}\n</code></pre> <p><code>PTN_LET(name, expr)</code> is the single-value shorthand, equivalent to <code>PTN_WHERE((name), expr)</code>:</p> <pre><code>using namespace ptn;\n\nconst char *bucket(int x) {\n  return match(x) | on(\n    $[PTN_LET(value, value &lt; 0)] &gt;&gt; \"negative\",\n    $[PTN_LET(value, value &lt; 10)] &gt;&gt; \"small\",\n    _ &gt;&gt; \"large\"\n  );\n}\n</code></pre> <p>Both macros expand to captureless guard callables and compose with <code>&amp;&amp;</code> / <code>||</code> like other guard predicates.</p>"},{"location":"changelog/v0.9.0/#api-cleanup-bind-and-_1_2_3-removed-breaking","title":"API Cleanup: <code>bind()</code> and <code>_1</code>/<code>_2</code>/<code>_3</code> Removed (Breaking)","text":"<p>The legacy <code>bind()</code> entry point and the positional guard aliases <code>_1</code>, <code>_2</code>, <code>_3</code> have been removed from the public surface.</p> <p>Migrate to the current equivalents:</p> Removed Replacement <code>bind()</code> <code>$</code> <code>bind(subpattern)</code> <code>$(subpattern)</code> <code>_1</code> <code>arg&lt;0&gt;</code> or <code>PTN_LET</code> <code>_2</code> <code>arg&lt;1&gt;</code> <code>_3</code> <code>arg&lt;2&gt;</code> <p>A compile-fail test covers both removed names to prevent silent regressions.</p>"},{"location":"changelog/v0.9.0/#callable-guard-fixes-for-msvc-and-standard-c","title":"Callable Guard Fixes for MSVC and Standard C++","text":"<p>Four guard fixes were shipped in this cycle:</p> <ul> <li>Callable guards evaluate as tuple predicates. The guard trait system   now classifies <code>callable_guard</code> correctly so <code>PTN_WHERE</code>/<code>PTN_LET</code>   participate in composed guard evaluation (<code>&amp;&amp;</code>, <code>||</code>) on all compilers.</li> <li>Replaced <code>std::apply</code> in tuple guard calls with an explicit index   sequence expansion to avoid MSVC ICEs on conformance-mode builds.</li> <li>Replaced <code>std::invoke</code> in callable guard dispatch with direct call   syntax for the same reason.</li> <li>MSVC traditional preprocessor workaround for <code>PTN_WHERE</code> argument   counting via <code>__VA_ARGS__</code> indirection.</li> </ul> <p>These fixes have no effect on GCC and Clang builds. MSVC users with <code>/Zc:preprocessor</code> enabled are not affected either.</p>"},{"location":"changelog/v0.9.0/#vcpkg-support","title":"vcpkg Support","text":"<p>Patternia is now available through the vcpkg package manager:</p> <pre><code>vcpkg install patternia\n</code></pre> <p>Or via <code>vcpkg.json</code> manifest:</p> <pre><code>{\n  \"dependencies\": [\"patternia\"]\n}\n</code></pre> <p>Consume through the existing CMake target:</p> <pre><code>find_package(patternia CONFIG REQUIRED)\ntarget_link_libraries(your_target PRIVATE patternia::patternia)\n</code></pre>"},{"location":"changelog/v0.9.0/#migration-notes","title":"Migration Notes","text":""},{"location":"changelog/v0.9.0/#bind-removed","title":"<code>bind()</code> removed","text":"<pre><code>// Before\nmatch(x) | on(\n  bind()[_0 &gt; 0] &gt;&gt; [](int v) { return v; },\n  _ &gt;&gt; 0\n);\n\n// After\nmatch(x) | on(\n  $[_0 &gt; 0] &gt;&gt; [](int v) { return v; },\n  _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"changelog/v0.9.0/#_1_2_3-removed","title":"<code>_1</code>/<code>_2</code>/<code>_3</code> removed","text":"<pre><code>// Before\n$(has&lt;&amp;Point::x, &amp;Point::y&gt;())[_1 == _2] &gt;&gt; \"diagonal\"\n\n// After\n$(has&lt;&amp;Point::x, &amp;Point::y&gt;())[arg&lt;0&gt; == arg&lt;1&gt;] &gt;&gt; \"diagonal\"\n// or\n$(has&lt;&amp;Point::x, &amp;Point::y&gt;())[PTN_WHERE((x, y), x == y)] &gt;&gt; \"diagonal\"\n</code></pre>"},{"location":"changelog/v0.9.0/#bug-fixes","title":"Bug Fixes","text":"<ul> <li>Fixed <code>PTN_WHERE</code> argument counting failing under MSVC traditional   preprocessor mode. Added <code>__VA_ARGS__</code> indirection as a standard   workaround. No effect on conforming preprocessors.</li> <li>Fixed callable guards failing to dispatch bound values correctly in   composed guard expressions by routing them through the tuple predicate   path instead of <code>std::invoke</code>.</li> <li>Fixed <code>std::apply</code>-based tuple guard calls producing ICEs on MSVC   conformance builds.</li> </ul>"},{"location":"changelog/v0.9.0/#internal","title":"Internal","text":"<ul> <li><code>include/ptn/pattern/modifiers/guard.hpp</code>: added <code>callable_guard</code> type   and <code>make_callable_guard</code>, rewrote tuple expansion without <code>std::apply</code>   or <code>std::invoke</code>.</li> <li><code>include/ptn/pattern/base/pattern_traits.hpp</code>, <code>modifiers/fwd.h</code>:   added <code>callable_guard</code> to the guard trait classifier.</li> <li><code>include/ptn/patternia.hpp</code>: added <code>PTN_WHERE</code> and <code>PTN_LET</code> macros,   removed <code>bind</code> and <code>_1</code>/<code>_2</code>/<code>_3</code> exports.</li> <li><code>tests/compile_fail/</code>: added <code>bind_function_removed.cpp</code> and   <code>guard_placeholder_alias_removed.cpp</code>.</li> </ul>"},{"location":"changelog/v0.9.1/","title":"Patternia v0.9.1 Release Note","text":"<p>Release Date: March 18, 2026 Version: 0.9.1</p>"},{"location":"changelog/v0.9.1/#overview","title":"Overview","text":"<p>Patternia v0.9.1 is an API refinement and packaging stabilization release.</p> <p>Compared with v0.9.0, this version finishes the roadmap transition from function-style static patterns to variable-template entry points, promotes the new structural and literal forms across the public docs, and tightens the vcpkg packaging flow for clean installs.</p>"},{"location":"changelog/v0.9.1/#new-features","title":"New Features","text":""},{"location":"changelog/v0.9.1/#valv-replaces-litv","title":"<code>val&lt;V&gt;</code> Replaces <code>lit&lt;V&gt;()</code>","text":"<p>Compile-time literal matching now uses <code>val&lt;V&gt;</code>:</p> <pre><code>match(x) | PTN_ON(\n  val&lt;1&gt; &gt;&gt; 1,\n  val&lt;2&gt; &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>This aligns static literal patterns with the existing variable-template style used by <code>is&lt;T&gt;</code> and <code>alt&lt;I&gt;</code>.</p> <p>Runtime literal patterns are unchanged:</p> <pre><code>match(x) | on(\n  lit(1) &gt;&gt; \"one\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre>"},{"location":"changelog/v0.9.1/#c20-floating-point-static-literals","title":"C++20 Floating-Point Static Literals","text":"<p>When you compile Patternia in C++20 or later mode, <code>val&lt;V&gt;</code> now accepts floating-point non-type template parameters:</p> <pre><code>match(x) | on(\n  val&lt;3.25&gt; &gt;&gt; \"three-point-two-five\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>In C++17 builds, floating-point static literals remain unavailable by design. Use <code>lit(3.25)</code> there.</p>"},{"location":"changelog/v0.9.1/#has-is-now-the-canonical-structural-entry","title":"<code>has&lt;...&gt;</code> Is Now the Canonical Structural Entry","text":"<p>Structural matching continues the same DSL direction as <code>val&lt;V&gt;</code>:</p> <pre><code>match(p) | on(\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) {\n    return x + y;\n  },\n  _ &gt;&gt; 0\n);\n</code></pre> <p>The old <code>has&lt;...&gt;()</code> factory-style spelling is no longer part of the public surface.</p>"},{"location":"changelog/v0.9.1/#vcpkg-packaging-follow-up","title":"vcpkg Packaging Follow-Up","text":"<p>This release keeps the vcpkg port aligned with the upstream install layout and documents vcpkg as the primary installation path. It also includes the cleanup needed for a clean package install after the initial v0.9.0 port landing.</p>"},{"location":"changelog/v0.9.1/#migration-notes","title":"Migration Notes","text":""},{"location":"changelog/v0.9.1/#litv-valv","title":"<code>lit&lt;V&gt;()</code> -&gt; <code>val&lt;V&gt;</code>","text":"<pre><code>// Before\nmatch(x) | on(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  lit&lt;2&gt;() &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n\n// After\nmatch(x) | on(\n  val&lt;1&gt; &gt;&gt; 1,\n  val&lt;2&gt; &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre>"},{"location":"changelog/v0.9.1/#has-has","title":"<code>has&lt;...&gt;()</code> -&gt; <code>has&lt;...&gt;</code>","text":"<pre><code>// Before\n$(has&lt;&amp;Point::x, &amp;Point::y&gt;()) &gt;&gt; [](int x, int y) {\n  return x + y;\n}\n\n// After\n$(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) {\n  return x + y;\n}\n</code></pre>"},{"location":"changelog/v0.9.1/#c17-vs-c20-static-floating-point-matching","title":"C++17 vs C++20 Static Floating-Point Matching","text":"<pre><code>// C++17\nmatch(x) | on(\n  lit(3.25) &gt;&gt; \"three-point-two-five\",\n  _ &gt;&gt; \"other\"\n);\n\n// C++20+\nmatch(x) | on(\n  val&lt;3.25&gt; &gt;&gt; \"three-point-two-five\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre>"},{"location":"changelog/v0.9.1/#internal","title":"Internal","text":"<ul> <li><code>include/ptn/pattern/lit.hpp</code>: replaced the compile-time literal factory   function with the <code>val&lt;V&gt;</code> variable template.</li> <li><code>include/ptn/patternia.hpp</code>: re-exported <code>val</code> through the umbrella header.</li> <li><code>tests/compile_fail/</code>: added regression coverage for removed <code>lit&lt;V&gt;()</code> and   <code>has&lt;...&gt;()</code> forms, plus pre-C++20 floating-point static literals.</li> <li><code>packaging/vcpkg/</code>: version metadata updated for the 0.9.1 release cycle.</li> </ul>"},{"location":"changelog/v0.9.2/","title":"Patternia v0.9.2 Release Note","text":"<p>Release Date: April 10, 2026 Version: 0.9.2</p>"},{"location":"changelog/v0.9.2/#overview","title":"Overview","text":"<p>Patternia v0.9.2 ships the <code>any(...)</code> / <code>all(...)</code> pattern combinators with comprehensive test coverage and API documentation.</p> <p>Compared with v0.9.1, this version stabilizes the combinator API first introduced in v0.9.1, expands test coverage across all pattern types, and adds the corresponding API reference documentation.</p>"},{"location":"changelog/v0.9.2/#new-features","title":"New Features","text":""},{"location":"changelog/v0.9.2/#anyps-or-pattern-combinator","title":"<code>any(ps...)</code> \u2014 OR Pattern Combinator","text":"<p>Matches when at least one sub-pattern matches. Short-circuits on the first match.</p> <pre><code>match(x) | on(\n  any(val&lt;1&gt;, val&lt;2&gt;, val&lt;3&gt;) &gt;&gt; \"1, 2, or 3\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Properties:</p> <ul> <li>Non-binding: handlers receive zero arguments.</li> <li>Sub-patterns are evaluated left-to-right; evaluation stops at the first   match.</li> <li>Requires at least one sub-pattern; every argument must be a pattern object.</li> </ul>"},{"location":"changelog/v0.9.2/#allps-and-pattern-combinator","title":"<code>all(ps...)</code> \u2014 AND Pattern Combinator","text":"<p>Matches only when every sub-pattern matches. Short-circuits on the first mismatch.</p> <pre><code>match(x) | on(\n  all(any(val&lt;1&gt;, val&lt;2&gt;), val&lt;2&gt;) &gt;&gt; \"2\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Properties:</p> <ul> <li>Non-binding: handlers receive zero arguments.</li> <li>Sub-patterns are evaluated left-to-right; evaluation stops at the first   mismatch.</li> <li>Requires at least one sub-pattern; every argument must be a pattern object.</li> </ul>"},{"location":"changelog/v0.9.2/#test-coverage","title":"Test Coverage","text":"<p>21 runtime tests and 4 compile-fail tests cover:</p> <ul> <li>Short-circuit evaluation (any stops on first hit, all stops on first miss).</li> <li>Composition with <code>val&lt;&gt;</code>, <code>lit()</code>, <code>lit_ci()</code>, and <code>is&lt;T&gt;</code>.</li> <li>Nested combinators (<code>any</code> inside <code>all</code> and vice versa).</li> <li>Single-subpattern edge cases.</li> <li>Pattern reuse across multiple matches.</li> <li><code>PTN_ON(...)</code> cached case pack integration.</li> <li>Static assertions for empty argument lists and non-pattern arguments.</li> </ul>"},{"location":"changelog/v0.9.2/#documentation","title":"Documentation","text":"<ul> <li>Added \"Pattern Combinators\" section to API reference (<code>docs/api.md</code>).</li> <li>Updated namespace summary to include <code>any</code> and <code>all</code>.</li> </ul>"},{"location":"changelog/v0.9.2/#internal","title":"Internal","text":"<ul> <li><code>tests/tests_combinator.cpp</code>: 16 new runtime tests added.</li> <li><code>tests/compile_fail/all_requires_pattern_args.cpp</code>: new compile-fail   regression test.</li> <li><code>tests/CMakeLists.txt</code>: registered new compile-fail test.</li> <li><code>docs/api.md</code>: added combinator section and namespace summary update.</li> </ul>"},{"location":"guide/getting-started/","title":"Getting Started","text":""},{"location":"guide/getting-started/#install","title":"Install","text":"<pre><code>git clone https://github.com/SentoMK/patternia.git\ncd patternia\ncmake -S . -B build -DCMAKE_BUILD_TYPE=Debug\ncmake --build build\n</code></pre> <p>Patternia is header-only. Including <code>ptn/patternia.hpp</code> is enough to use the library.</p>"},{"location":"guide/getting-started/#first-match","title":"First Match","text":"<pre><code>#include &lt;ptn/patternia.hpp&gt;\n\nint classify(int x) {\n  using namespace ptn;\n\n  return match(x) | on(\n    lit(0) &gt;&gt; 0,\n    lit(5) &gt;&gt; 5,\n    _ &gt;&gt; -1\n  );\n}\n</code></pre> <p>Read this as:</p> <ol> <li>Start matching <code>x</code>.</li> <li>Try the cases in order.</li> <li>Use <code>_</code> for the final fallback.</li> </ol>"},{"location":"guide/getting-started/#side-effects-and-returned-values","title":"Side Effects and Returned Values","text":"<p>Patternia matches are expressions. Handlers can return values or perform side effects.</p> <pre><code>using namespace ptn;\n\nvoid log_value(int x) {\n  match(x) | on(\n    lit(1) &gt;&gt; [] { std::cout &lt;&lt; \"one\\n\"; },\n    lit(2) &gt;&gt; [] { std::cout &lt;&lt; \"two\\n\"; },\n    _ &gt;&gt; [] { std::cout &lt;&lt; \"other\\n\"; }\n  );\n}\n</code></pre> <pre><code>using namespace ptn;\n\nint score(int x) {\n  return match(x) | on(\n    lit(1) &gt;&gt; 100,\n    lit(2) &gt;&gt; 200,\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"guide/getting-started/#binding","title":"Binding","text":"<p>Use <code>$</code> or <code>$(...)</code> when the handler needs data from the match.</p> <pre><code>using namespace ptn;\n\nint identity(int x) {\n  return match(x) | on(\n    $ &gt;&gt; [](int v) { return v; },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre> <p>Use <code>$</code> to bind the whole subject. Use <code>$(...)</code> to bind under a subpattern.</p>"},{"location":"guide/getting-started/#guards","title":"Guards","text":"<p>Attach a guard with <code>[]</code>.</p> <pre><code>using namespace ptn;\n\nconst char *bucket(int x) {\n  return match(x) | on(\n    $[PTN_LET(value, value &gt; 0 &amp;&amp; value &lt; 10)] &gt;&gt; \"small\",\n    _ &gt;&gt; \"other\"\n  );\n}\n</code></pre> <p>For multiple bound values, use <code>arg&lt;N&gt;</code> or <code>PTN_WHERE((...), expr)</code>:</p> <pre><code>using namespace ptn;\n\nstruct Point {\n  int x;\n  int y;\n};\n\nbool on_unit_circle(const Point &amp;p) {\n  return match(p) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;)[arg&lt;0&gt; * arg&lt;0&gt; + arg&lt;1&gt; * arg&lt;1&gt; == 1]\n        &gt;&gt; true,\n    _ &gt;&gt; false\n  );\n}\n</code></pre> <pre><code>using namespace ptn;\n\nbool on_diagonal(const Point &amp;p) {\n  return match(p) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;)[PTN_WHERE((x, y), x == y)] &gt;&gt; true,\n    _ &gt;&gt; false\n  );\n}\n</code></pre>"},{"location":"guide/getting-started/#structural-matching","title":"Structural Matching","text":"<p>Use <code>has&lt;&gt;</code> to describe object structure and <code>$(...)</code> to bind selected members.</p> <pre><code>using namespace ptn;\n\nstruct Point {\n  int x;\n  int y;\n};\n\nint sum(const Point &amp;p) {\n  return match(p) | on(\n    $(has&lt;&amp;Point::x, &amp;Point::y&gt;) &gt;&gt; [](int x, int y) {\n      return x + y;\n    },\n    _ &gt;&gt; 0\n  );\n}\n</code></pre>"},{"location":"guide/getting-started/#variant-matching","title":"Variant Matching","text":"<p>Use <code>is&lt;T&gt;</code> for type-based dispatch and <code>alt&lt;I&gt;</code> for index-based dispatch.</p> <pre><code>using namespace ptn;\n\nusing Value = std::variant&lt;int, std::string&gt;;\n\nstd::string describe(const Value &amp;v) {\n  return match(v) | on(\n    is&lt;int&gt; &gt;&gt; \"int\",\n    $(is&lt;std::string&gt;) &gt;&gt; [](const std::string &amp;s) {\n      return \"str:\" + s;\n    },\n    _ &gt;&gt; [] { return std::string(\"other\"); }\n  );\n}\n</code></pre>"},{"location":"guide/getting-started/#cached-hot-paths","title":"Cached Hot Paths","text":"<p>If the same case pack is reused on a hot path, cache it:</p> <pre><code>using namespace ptn;\n\nint classify_fast(int x) {\n  return match(x) | PTN_ON(\n    val&lt;1&gt; &gt;&gt; 1,\n    val&lt;2&gt; &gt;&gt; 2,\n    _ &gt;&gt; 0\n  );\n}\n</code></pre> <p>Or:</p> <pre><code>using namespace ptn;\n\nint classify_fast(int x) {\n  return match(x) | static_on([] {\n    return on(\n      val&lt;1&gt; &gt;&gt; 1,\n      val&lt;2&gt; &gt;&gt; 2,\n      _ &gt;&gt; 0\n    );\n  });\n}\n</code></pre>"},{"location":"guide/getting-started/#tests","title":"Tests","text":"<pre><code>cmake -S . -B build -DPTN_BUILD_TESTS=ON\ncmake --build build --target ptn_tests\nctest --test-dir build --output-on-failure\n</code></pre>"},{"location":"guide/getting-started/#next","title":"Next","text":"<ul> <li>Read the API reference</li> <li>Read the design overview</li> <li>Continue with the tutorials under <code>docs/tutorials/</code></li> </ul>"},{"location":"guide/installation/","title":"Installation","text":"<p>Patternia is a header-only C++ library. There is no build system, no linking step, and no external dependency.</p> <p>You can integrate Patternia into your project in any of the following ways.</p>"},{"location":"guide/installation/#1-vcpkg","title":"1. vcpkg","text":"<pre><code>vcpkg install patternia\n</code></pre> <p>Or via <code>vcpkg.json</code> manifest:</p> <pre><code>{\n  \"dependencies\": [\"patternia\"]\n}\n</code></pre> <p>Then in your CMake project:</p> <pre><code>find_package(patternia CONFIG REQUIRED)\ntarget_link_libraries(your_target PRIVATE patternia::patternia)\n</code></pre> <p>Pass the vcpkg toolchain file to CMake if it is not already in your preset:</p> <pre><code>cmake -B build -DCMAKE_TOOLCHAIN_FILE=/path/to/vcpkg/scripts/buildsystems/vcpkg.cmake\n</code></pre>"},{"location":"guide/installation/#2-fetch-patternia-automatically","title":"2. Fetch Patternia automatically","text":"<p>You may choose to fetch Patternia as a remote dependency during configuration.</p> <p>FetchContent:</p> <pre><code>include(FetchContent)\nFetchContent_Declare(patternia\n  GIT_REPOSITORY https://github.com/sentomk/patternia.git\n  GIT_TAG main\n)\nFetchContent_MakeAvailable(patternia)\n\ntarget_link_libraries(your_target PRIVATE patternia::patternia)\n</code></pre> <p>CPM.cmake:</p> <pre><code>CPMAddPackage(\"gh:sentomk/patternia@main\")\n</code></pre> <p>This fits well in reproducible CI pipelines and modern CMake workflows.</p>"},{"location":"guide/installation/#3-install-and-consume-via-find_package","title":"3. Install and consume via <code>find_package()</code>","text":"<p>Patternia provides full CMake package export support.</p> <p>Install:</p> <pre><code>cmake -B build\ncmake --build build\ncmake --install build --prefix /path/to/install\n</code></pre> <p>Use in any project:</p> <pre><code>find_package(patternia REQUIRED)\ntarget_link_libraries(your_target PRIVATE patternia::patternia)\n</code></pre> <p>This is the recommended method for system-wide integration, packaging, and distribution.</p>"},{"location":"guide/installation/#4-add-patternia-as-a-git-submodule","title":"4. Add Patternia as a Git submodule","text":"<pre><code>git submodule add https://github.com/sentomk/patternia extern/patternia\n</code></pre> <p>Then in your CMake project:</p> <pre><code>add_subdirectory(extern/patternia)\ntarget_link_libraries(your_target PRIVATE patternia::patternia)\n</code></pre> <p>This approach keeps Patternia version-controlled inside your repository and works well for mid- to large-scale projects.</p>"},{"location":"guide/installation/#5-include-the-include-directory-directly","title":"5. Include the <code>include/</code> directory directly","text":"<p>This is the simplest approach. Just add Patternia\u2019s header path to your target:</p> <pre><code>target_include_directories(your_target\n  PRIVATE /path/to/patternia/include\n)\n</code></pre> <p>No configuration, no installation, immediately usable.</p>"},{"location":"performance/","title":"Performance Notes","text":"<p>This section tracks performance-oriented algorithm evolution by version.</p>"},{"location":"performance/#current-version","title":"Current Version","text":"<p>v0.8.2 established the lowering engine baseline. See v0.8.2 performance note for benchmark data and algorithm details.</p> <p>Releases from v0.8.3 onward focus on guard fixes, API ergonomics, and compiler compatibility. No new dispatch algorithms were introduced.</p>"},{"location":"performance/#version-index","title":"Version Index","text":"<ul> <li>v0.8.2 (lowering engine baseline)</li> <li>v0.8.0 (tiered variant dispatch)</li> </ul>"},{"location":"performance/#scope","title":"Scope","text":"<p>Performance notes focus on:</p> <ul> <li>dispatch strategy evolution</li> <li>fast-path and cold-path behavior</li> <li>algorithm-level tradeoffs and applicability</li> </ul> <p>For API compatibility and migration notes, see:</p> <ul> <li>Changelog Releases</li> <li>API Reference</li> </ul>"},{"location":"performance/v0.8.0/","title":"Patternia Performance - v0.8.0","text":""},{"location":"performance/v0.8.0/#overview","title":"Overview","text":"<p>v0.8.0 introduces a tiered variant dispatch model and related runtime/layout optimizations for large-branch workloads.</p>"},{"location":"performance/v0.8.0/#core-algorithms","title":"Core Algorithms","text":""},{"location":"performance/v0.8.0/#1-tiered-variant-dispatch","title":"1. Tiered Variant Dispatch","text":"<p>Variant dispatch is selected by alternative count:</p> <ul> <li><code>hot_inline</code> for small alt counts</li> <li><code>warm_segmented</code> for medium alt counts</li> <li><code>cold_compact</code> for large alt counts</li> </ul> <p>This reduces hot-path code pressure while preserving fast dispatch for common small/medium cases.</p>"},{"location":"performance/v0.8.0/#2-compact-alt-map-for-large-variant-sets","title":"2. Compact Alt Map for Large Variant Sets","text":"<p>For large variant spaces, dispatch uses:</p> <ul> <li><code>active_index -&gt; compact slot</code> map</li> <li>miss -&gt; fallback path</li> <li>hit -&gt; dense trampoline table</li> </ul> <p>This avoids scanning irrelevant alternatives and keeps large dispatch data compact.</p>"},{"location":"performance/v0.8.0/#3-per-alt-case-range-narrowing-typed-variant","title":"3. Per-Alt Case Range Narrowing (Typed Variant)","text":"<p>Typed variant dispatch computes case ranges per active alternative, then scans only the relevant range instead of the full case set.</p>"},{"location":"performance/v0.8.0/#4-hotcold-path-separation","title":"4. Hot/Cold Path Separation","text":"<p>Large-branch dispatch work is moved into non-inline cold helpers to reduce instruction footprint in hot entry paths.</p>"},{"location":"performance/v0.8.0/#implementation-pointers","title":"Implementation Pointers","text":"<ul> <li><code>include/ptn/core/common/optimize.hpp</code>: dispatch tiers and metadata builders</li> <li><code>include/ptn/core/common/eval.hpp</code>: tier selection and runtime dispatch execution</li> </ul>"},{"location":"performance/v0.8.0/#notes","title":"Notes","text":"<ul> <li>This page records algorithm strategy for v0.8.0.</li> <li>API-level changes are documented in: v0.8.0</li> </ul>"},{"location":"performance/v0.8.2/","title":"Patternia Performance - v0.8.2","text":"<p>Published: 2026-03-03</p> <p>Focus:</p> <ul> <li>establish the lowering engine as the main optimization architecture</li> <li>push static literal dispatch closer to the <code>switch</code> baseline</li> <li>separate dispatch quality from matcher-construction overhead</li> </ul>"},{"location":"performance/v0.8.2/#summary","title":"Summary","text":"<p>v0.8.2 moves Patternia's performance work from isolated fast paths to a general lowering engine.</p> <p>This version focuses on one practical target:</p> <ul> <li>make keyed pattern chains behave as closely as possible to hand-written   <code>switch</code> code</li> <li>keep the optimization scalable beyond one special-case pattern family</li> <li>reduce the matcher-construction overhead that appears in   <code>match(x) | on(...)</code></li> </ul> <p>The result is a two-part improvement:</p> <ul> <li>the compiler-visible static literal path is now close to the <code>switch</code>   baseline</li> <li>the pipeline syntax can reuse a cached matcher through <code>static_on(...)</code> or   <code>PTN_ON(...)</code></li> </ul> <p>The benchmark discussion in this note uses the 128-way literal scenario because it is large enough to expose both sides of the problem:</p> <ul> <li>how close lowering gets to hand-written control flow</li> <li>how expensive raw inline matcher construction still is</li> </ul>"},{"location":"performance/v0.8.2/#benchmark-snapshot","title":"Benchmark Snapshot","text":""},{"location":"performance/v0.8.2/#raw-pipeline-form-vs-switch","title":"Raw pipeline form vs <code>switch</code>","text":"<p>This chart answers the simple question: what happens if you write the direct pipeline form and call it in a hot loop?</p> <p></p>"},{"location":"performance/v0.8.2/#cached-matcher-forms-vs-switch","title":"Cached matcher forms vs <code>switch</code>","text":"<p>This chart isolates the same 128-way static-literal workload after matcher construction is moved out of the hot path.</p> <p></p>"},{"location":"performance/v0.8.2/#problems-we-needed-to-solve","title":"Problems We Needed To Solve","text":""},{"location":"performance/v0.8.2/#problem-1-runtime-literal-factories-hide-the-dispatch-key","title":"Problem 1: Runtime literal factories hide the dispatch key","text":"<p><code>lit(value)</code> is a general runtime factory.</p> <p>That is the right API for dynamic values and strings, but it means the lowering stage cannot see the literal as a compile-time key:</p> <pre><code>match(x) | on(\n  lit(1) &gt;&gt; 1,\n  lit(2) &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>For this shape, Patternia can still build a specialized linear literal path, but it cannot reliably target switch-like lowering.</p>"},{"location":"performance/v0.8.2/#problem-2-matchx-on-pays-matcher-construction-cost-every-call","title":"Problem 2: <code>match(x) | on(...)</code> pays matcher construction cost every call","text":"<p>Even when the match logic itself lowers well, the inline pipeline form still constructs the whole matcher object on every evaluation:</p> <pre><code>match(x) | on(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  lit&lt;2&gt;() &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>For large case sets, this construction cost dominates the dispatch cost.</p>"},{"location":"performance/v0.8.2/#problem-3-optimization-logic-did-not-scale","title":"Problem 3: Optimization logic did not scale","text":"<p>Before v0.8.2, performance logic was drifting toward isolated special cases in <code>optimize.hpp</code> and <code>eval.hpp</code>.</p> <p>That approach does not scale for future keyed scenarios such as:</p> <ul> <li>static literals</li> <li>simple variant alternative dispatch</li> <li>guarded variant cases that still expose a primary key</li> </ul> <p>Patternia needed a shared lowering model, not one more fast path.</p>"},{"location":"performance/v0.8.2/#initial-solution","title":"Initial Solution","text":"<p>The first step was to expose a compile-time literal form:</p> <pre><code>lit&lt;42&gt;()\n</code></pre> <p>This gives the lowering stage a stable key that can move into type-level metadata and plan selection.</p> <p>The second step was to validate the idea with a narrow benchmark:</p> <ul> <li>128 static literal cases</li> <li>one trailing wildcard default</li> <li>direct comparison against a hand-written <code>switch</code></li> </ul> <p>That experiment confirmed two separate facts:</p> <ul> <li>static keyed lowering works</li> <li>raw <code>match(x) | on(...)</code> still loses badly if matcher construction remains in   the hot path</li> </ul> <p>That is why v0.8.2 treats lowering and matcher reuse as separate concerns.</p>"},{"location":"performance/v0.8.2/#lowering-engine-architecture","title":"Lowering Engine Architecture","text":"<p>v0.8.2 organizes the optimizer around a small internal pipeline:</p> <pre><code>case DSL\n  -&gt; case_analysis\n  -&gt; case_sequence_ir\n  -&gt; lowering_analysis\n  -&gt; dispatch_plan\n  -&gt; eval_cases_with_dispatch_plan(...)\n</code></pre> <p>The main pieces are:</p> Component Responsibility <code>case_analysis</code> Analyze one case into discriminator, residual work, and binding shape <code>case_sequence_ir</code> Aggregate the full case chain and decide whether lowering is legal <code>lowering_analysis</code> Expose legality and plan-family signals to the planner <code>dispatch_plan</code> Carry the concrete execution shape selected for the evaluator <code>eval_cases_with_dispatch_plan(...)</code> Execute the chosen plan without re-running planner logic <p>The design is built around three legality grades:</p> Rule Meaning Current result <code>full</code> The key alone can drive the dispatch path direct lowering <code>bucketed</code> The key narrows the search, but some residual replay remains per-key bucket replay <code>none</code> The planner cannot prove a safe keyed path sequential evaluation <p>In code, this lives in:</p> <ul> <li>optimize.hpp</li> <li>eval.hpp</li> </ul>"},{"location":"performance/v0.8.2/#algorithms-and-principles","title":"Algorithms and Principles","text":""},{"location":"performance/v0.8.2/#1-dense-static-literal-lowering","title":"1. Dense static-literal lowering","text":"<p>This is the main new <code>full</code> path in v0.8.2.</p> <p>Patternia first checks whether the case chain is a good dense-key candidate. The current heuristics are:</p> <ul> <li>subject type is integral or enum</li> <li>every case is a zero-bind static literal or the trailing wildcard default</li> <li>case count is at most <code>256</code></li> <li>key span is at most <code>512</code></li> <li>key span is dense enough relative to case count</li> </ul> <p>If those checks pass, the planner selects:</p> <ul> <li><code>dispatch_plan_kind::static_literal_dense</code></li> </ul> <p>The metadata computes:</p> <ul> <li><code>min_value</code></li> <li><code>max_value</code></li> <li><code>range_size</code></li> <li><code>default_case_index</code></li> <li>compile-time <code>case_index_table</code></li> </ul> <p>At runtime, the hot path becomes:</p> <pre><code>value\n  -&gt; range check\n  -&gt; offset = value - min_value\n  -&gt; switch(offset)\n  -&gt; selected case entry or default\n</code></pre>"},{"location":"performance/v0.8.2/#example-sparse-values-inside-a-small-span","title":"Example: sparse values inside a small span","text":"<p>For:</p> <pre><code>match(x) | on(\n  lit&lt;1&gt;()  &gt;&gt; 1,\n  lit&lt;2&gt;()  &gt;&gt; 2,\n  lit&lt;10&gt;() &gt;&gt; 10,\n  _        &gt;&gt; 0\n);\n</code></pre> <p>Patternia computes:</p> <ul> <li><code>min_value = 1</code></li> <li><code>max_value = 10</code></li> <li><code>range_size = 10</code></li> </ul> <p>The dense metadata conceptually maps offsets like this:</p> Value Offset Selected branch <code>1</code> <code>0</code> case <code>lit&lt;1&gt;()</code> <code>2</code> <code>1</code> case <code>lit&lt;2&gt;()</code> <code>3..9</code> <code>2..8</code> default <code>10</code> <code>9</code> case <code>lit&lt;10&gt;()</code> <p>This is why the generated path is much closer to <code>switch</code> than a generic pattern replay loop.</p>"},{"location":"performance/v0.8.2/#2-variant-keyed-dispatch-with-bucket-replay","title":"2. Variant keyed dispatch with bucket replay","text":"<p>The lowering engine is not literal-only.</p> <p>Variant dispatch is now expressed through the same planner structure:</p> <ul> <li><code>variant_simple</code></li> <li><code>variant_alt_bucketed</code></li> </ul> <p>The <code>full</code> case is straightforward:</p> <pre><code>match(v) | on(\n  alt&lt;0&gt; &gt;&gt; 1,\n  alt&lt;1&gt; &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>The active alternative is already the dispatch key, so no residual work is needed.</p> <p>The <code>bucketed</code> case appears when the alternative is still useful, but one key maps to multiple residual checks:</p> <pre><code>match(v) | on(\n  $(is&lt;int&gt;)[_0 &gt; 100] &gt;&gt; 10,\n  is&lt;int&gt;              &gt;&gt; 1,\n  is&lt;std::string&gt;      &gt;&gt; 2,\n  _                       &gt;&gt; 0\n);\n</code></pre> <p>Here, the active <code>variant</code> alternative narrows the search immediately:</p> <ul> <li><code>int</code> -&gt; replay only the <code>int</code> bucket</li> <li><code>std::string</code> -&gt; replay only the string bucket</li> </ul> <p>This preserves first-match semantics while avoiding a full-chain scan.</p>"},{"location":"performance/v0.8.2/#3-matcher-reuse-for-pipeline-syntax","title":"3. Matcher reuse for pipeline syntax","text":"<p>Lowering alone is not enough if the matcher object is rebuilt every time.</p> <p>That is why v0.8.2 also adds a reusable pipeline form:</p> <pre><code>match(x) | PTN_ON(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  lit&lt;2&gt;() &gt;&gt; 2,\n  _ &gt;&gt; 0\n);\n</code></pre> <p><code>PTN_ON(...)</code> is a convenience wrapper around:</p> <pre><code>match(x) | static_on([] {\n  return on(\n    lit&lt;1&gt;() &gt;&gt; 1,\n    lit&lt;2&gt;() &gt;&gt; 2,\n    _ &gt;&gt; 0\n  );\n});\n</code></pre> <p>This keeps the pipeline syntax while moving matcher construction out of the hot path.</p>"},{"location":"performance/v0.8.2/#worked-example-why-raw-on-is-still-slow","title":"Worked Example: Why Raw <code>on(...)</code> Is Still Slow","text":"<p>Consider the three 128-way forms used by the benchmark:</p>"},{"location":"performance/v0.8.2/#inline-matcher","title":"Inline matcher","text":"<pre><code>match(x) | on(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  // ...\n  lit&lt;128&gt;() &gt;&gt; 128,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>This form pays for:</p> <ul> <li>case object construction</li> <li>value-handler object construction</li> <li><code>on(...)</code> tuple construction</li> </ul> <p>on every call.</p>"},{"location":"performance/v0.8.2/#manually-cached-matcher","title":"Manually cached matcher","text":"<pre><code>static auto cached_cases = on(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  // ...\n  lit&lt;128&gt;() &gt;&gt; 128,\n  _ &gt;&gt; 0\n);\n\nreturn match(x) | cached_cases;\n</code></pre> <p>This keeps the lowering path identical, but it removes repeated matcher construction.</p>"},{"location":"performance/v0.8.2/#cached-pipeline-matcher","title":"Cached pipeline matcher","text":"<pre><code>match(x) | PTN_ON(\n  lit&lt;1&gt;() &gt;&gt; 1,\n  // ...\n  lit&lt;128&gt;() &gt;&gt; 128,\n  _ &gt;&gt; 0\n);\n</code></pre> <p>This is the same reuse idea packaged for pipeline syntax.</p>"},{"location":"performance/v0.8.2/#final-results","title":"Final Results","text":"<p>The following numbers come from the current local run of:</p> <pre><code>.\\build\\bench\\ptn_bench_lit.exe --benchmark_filter=\"LiteralMatch128\" --benchmark_min_time=0.5s\n</code></pre> Benchmark Meaning CPU time <code>BM_PatterniaPipe_LiteralMatch128StaticCases</code> prebuilt matcher object <code>1.74 ns</code> <code>BM_PatterniaPipe_LiteralMatch128On</code> raw inline <code>on(...)</code> <code>17.9 ns</code> <code>BM_PatterniaPipe_LiteralMatch128OnMacro</code> cached pipeline matcher via <code>PTN_ON(...)</code> <code>2.09 ns</code> <code>BM_Switch_LiteralMatch128</code> hand-written <code>switch</code> baseline <code>1.52 ns</code>"},{"location":"performance/v0.8.2/#what-these-numbers-mean","title":"What these numbers mean","text":"<ul> <li>The static literal lowering itself is close to the <code>switch</code> baseline.</li> <li>The dominant remaining gap in the raw pipeline form is matcher construction,   not keyed dispatch.</li> <li><code>PTN_ON(...)</code> recovers most of the lost performance while keeping the   pipeline syntax.</li> </ul> <p>In practice, the current result is:</p> <ul> <li><code>StaticCases</code> is about <code>1.14x</code> the <code>switch</code> baseline</li> <li><code>PTN_ON(...)</code> is about <code>1.37x</code> the <code>switch</code> baseline</li> <li>raw <code>on(...)</code> is roughly <code>11.8x</code> slower than the <code>switch</code> baseline in this   benchmark because it rebuilds the matcher every call</li> </ul>"},{"location":"performance/v0.8.2/#outcome","title":"Outcome","text":"<p>v0.8.2 establishes the lowering engine as an internal architecture, not just a collection of local optimizations.</p> <p>It delivers five concrete outcomes:</p> <ol> <li><code>lit&lt;value&gt;()</code> now has a real switch-oriented lowering path.</li> <li>The engine has a shared legality model: <code>full</code>, <code>bucketed</code>, and <code>none</code>.</li> <li>Variant dispatch and static literal dispatch now live under the same plan    model.</li> <li><code>PTN_ON(...)</code> provides a practical pipeline form that avoids repeated    matcher construction.</li> <li>The performance gap to hand-written <code>switch</code> is now small enough that    future work can focus on fewer, more specific remaining costs.</li> </ol> <p>For API-level release notes, see v0.8.2.</p>"},{"location":"tutorials/from-control-flow/","title":"From Control Flow to Pattern Matching","text":"<p>Patternia is useful when branching logic wants to be read as a list of cases instead of a sequence of checks.</p>"},{"location":"tutorials/from-control-flow/#value-dispatch","title":"Value Dispatch","text":"<p>A simple <code>switch</code>-style decision maps directly to ordered cases:</p> <pre><code>auto describe(int x) {\n  using namespace ptn;\n\n  return match(x) | on(\n    lit(0) &gt;&gt; \"zero\",\n    lit(1) &gt;&gt; \"one\",\n    _ &gt;&gt; \"many\"\n  );\n}\n</code></pre>"},{"location":"tutorials/from-control-flow/#constraint-based-dispatch","title":"Constraint-Based Dispatch","text":"<p>Once ordering and overlapping conditions matter, bind the subject explicitly and attach guards:</p> <pre><code>auto bucket(int x) {\n  using namespace ptn;\n\n  return match(x) | on(\n    $[_0 &lt; 0] &gt;&gt; 0,\n    $[_0 &lt; 10] &gt;&gt; 1,\n    $[_0 &lt; 100] &gt;&gt; 2,\n    _ &gt;&gt; 3\n  );\n}\n</code></pre>"},{"location":"tutorials/from-control-flow/#structured-data","title":"Structured Data","text":"<p>Patternia keeps structure, constraints, and behavior in one visible case list:</p> <pre><code>struct User {\n  int age;\n  bool active;\n};\n\nauto label(const User &amp;u) {\n  using namespace ptn;\n\n  return match(u) | on(\n    $(has&lt;&amp;User::active&gt;)[arg&lt;0&gt; == false] &gt;&gt; \"inactive\",\n    $(has&lt;&amp;User::age&gt;)[arg&lt;0&gt; &lt; 18] &gt;&gt; \"minor\",\n    _ &gt;&gt; \"adult\"\n  );\n}\n</code></pre>"},{"location":"tutorials/from-control-flow/#takeaway","title":"Takeaway","text":"<p>The shift is simple:</p> <ul> <li>use literal cases for value dispatch</li> <li>use explicit binding and guards for constrained cases</li> <li>keep the fallback visible as <code>_</code></li> </ul>"},{"location":"tutorials/other-languages/","title":"Pattern Matching in Other Languages","text":"<p>If you already know Rust, Scala, Haskell, or OCaml, Patternia should feel familiar at the semantic level:</p> <ul> <li>matching is expression-oriented</li> <li>cases are ordered</li> <li>the first matching case wins</li> <li>fallback is explicit</li> </ul>"},{"location":"tutorials/other-languages/#expression-oriented-matching","title":"Expression-Oriented Matching","text":"<p>Rust:</p> <pre><code>let label = match x {\n    0 =&gt; \"zero\",\n    1 =&gt; \"one\",\n    _ =&gt; \"many\",\n};\n</code></pre> <p>Patternia:</p> <pre><code>auto label = match(x) | on(\n  lit(0) &gt;&gt; \"zero\",\n  lit(1) &gt;&gt; \"one\",\n  _ &gt;&gt; \"many\"\n);\n</code></pre>"},{"location":"tutorials/other-languages/#explicit-binding","title":"Explicit Binding","text":"<p>Many languages bind inside the pattern syntax. Patternia keeps binding explicit because it is a C++ library:</p> <pre><code>match(value) | on(\n  $ &gt;&gt; [](auto v) { process(v); },\n  _ &gt;&gt; [] {}\n);\n</code></pre>"},{"location":"tutorials/other-languages/#guards","title":"Guards","text":"<p>Patternia guards refine a bound case:</p> <pre><code>auto label = match(x) | on(\n  $[PTN_LET(value, value &gt; 0 &amp;&amp; value &lt; 10)] &gt;&gt; \"small\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre>"},{"location":"tutorials/other-languages/#structural-matching","title":"Structural Matching","text":"<p>Without native language destructuring, Patternia uses explicit member pointers:</p> <pre><code>auto label = match(user) | on(\n  $(has&lt;&amp;User::age, &amp;User::active&gt;)[arg&lt;0&gt; &lt; 18 &amp;&amp; arg&lt;1&gt; == true]\n      &gt;&gt; \"minor\",\n  _ &gt;&gt; \"adult\"\n);\n</code></pre>"},{"location":"tutorials/other-languages/#mental-model","title":"Mental Model","text":"<p>Patternia does not try to copy another language's syntax. It translates the same ideas into a small C++ DSL:</p> <ul> <li>explicit subject</li> <li>explicit bindings</li> <li>explicit fallback</li> </ul>"},{"location":"tutorials/policy-constraint/","title":"Policy Constraint Matching","text":"<p>Policy logic is usually a sequence of overlapping conditions. Patternia turns those rules into an ordered case list.</p> <pre><code>enum class Role {\n  Guest,\n  User,\n  Admin\n};\n\nstruct Request {\n  Role role;\n  int  resource_id;\n  bool read_only;\n};\n</code></pre>"},{"location":"tutorials/policy-constraint/#naming-domain-predicates","title":"Naming Domain Predicates","text":"<pre><code>auto is_public_resource = [](int id) {\n  return id &gt;= 0 &amp;&amp; id &lt; 1000;\n};\n</code></pre>"},{"location":"tutorials/policy-constraint/#declaring-the-rules","title":"Declaring the Rules","text":"<pre><code>bool allowed(const Request &amp;req) {\n  using namespace ptn;\n\n  return match(req) | on(\n    $(has&lt;&amp;Request::role&gt;)[arg&lt;0&gt; == Role::Admin] &gt;&gt; true,\n    $(has&lt;&amp;Request::role, &amp;Request::resource_id, &amp;Request::read_only&gt;)\n        [arg&lt;0&gt; == Role::User &amp;&amp; arg&lt;1&gt; &gt;= 0 &amp;&amp; arg&lt;2&gt; == true] &gt;&gt; true,\n    $(has&lt;&amp;Request::role, &amp;Request::resource_id&gt;)\n        [arg&lt;0&gt; == Role::Guest &amp;&amp; is_public_resource] &gt;&gt; true,\n    _ &gt;&gt; false\n  );\n}\n</code></pre>"},{"location":"tutorials/policy-constraint/#why-this-helps","title":"Why This Helps","text":"<p>Each rule is self-contained:</p> <ul> <li>the binding states which fields matter</li> <li>the guard states why the rule applies</li> <li>the handler states the outcome</li> </ul> <p>That keeps policy code readable even when ordering matters.</p>"},{"location":"tutorials/predicate-guards/","title":"Custom Predicate Guards","text":"<p>Guards refine an already-bound case. In Patternia, they live inside the case definition:</p> <pre><code>match(x) | on(\n  $[_0 &gt; 0 &amp;&amp; _0 &lt; 10] &gt;&gt; \"small\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre>"},{"location":"tutorials/predicate-guards/#single-value-guards","title":"Single-Value Guards","text":"<p>Use <code>_0</code> for a case that binds exactly one value.</p> <pre><code>match(x) | on(\n  $[_0 == 0 || _0 == 1] &gt;&gt; \"edge\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>If you want a name instead of <code>_0</code>, use <code>PTN_LET(name, expr)</code>:</p> <pre><code>match(x) | on(\n  $[PTN_LET(value, value == 0 || value == 1)] &gt;&gt; \"edge\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>Use <code>rng(...)</code> when a range reads better than two comparisons:</p> <pre><code>match(x) | on(\n  $[rng(0, 10)] &gt;&gt; \"closed\",\n  $[rng(0, 10, pat::mod::open)] &gt;&gt; \"open\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre>"},{"location":"tutorials/predicate-guards/#multi-value-guards","title":"Multi-Value Guards","text":"<p>Use <code>arg&lt;N&gt;</code> when a pattern binds multiple values.</p> <pre><code>struct Point {\n  int x;\n  int y;\n};\n\nmatch(p) | on(\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;)[arg&lt;0&gt; == arg&lt;1&gt;] &gt;&gt; \"diagonal\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre> <p>If you prefer named guard parameters, use <code>PTN_WHERE((...), expr)</code>:</p> <pre><code>match(p) | on(\n  $(has&lt;&amp;Point::x, &amp;Point::y&gt;)[PTN_WHERE((x, y), x == y)] &gt;&gt; \"diagonal\",\n  _ &gt;&gt; \"other\"\n);\n</code></pre>"},{"location":"tutorials/predicate-guards/#use-lambdas-for-domain-logic","title":"Use Lambdas for Domain Logic","text":"<p>When the rule is not a short relational expression, use a predicate.</p> <pre><code>auto is_prime = [](int v) {\n  if (v &lt; 2) {\n    return false;\n  }\n  for (int i = 2; i * i &lt;= v; ++i) {\n    if (v % i == 0) {\n      return false;\n    }\n  }\n  return true;\n};\n\nmatch(x) | on(\n  $[is_prime] &gt;&gt; \"prime\",\n  _ &gt;&gt; \"composite\"\n);\n</code></pre> <p>You can mix a placeholder expression with a named predicate:</p> <pre><code>auto valid_id = [](int v) {\n  return v % 7 == 0;\n};\n\nmatch(x) | on(\n  $[_0 &gt; 0 &amp;&amp; valid_id] &gt;&gt; \"valid\",\n  _ &gt;&gt; \"invalid\"\n);\n</code></pre>"},{"location":"tutorials/predicate-guards/#guideline","title":"Guideline","text":"<p>Use placeholder expressions for short, local constraints. Use lambdas for logic that deserves a name.</p>"},{"location":"tutorials/sphere-constraint/","title":"Geometric Constraint Matching","text":"<p>Geometric rules are often clearer as named constraints than as inline <code>if</code> statements.</p> <pre><code>struct Vec3 {\n  double x;\n  double y;\n  double z;\n};\n</code></pre>"},{"location":"tutorials/sphere-constraint/#name-the-predicate","title":"Name the Predicate","text":"<pre><code>auto inside_unit_sphere = [](double x, double y, double z) {\n  return x * x + y * y + z * z &lt; 1.0;\n};\n</code></pre>"},{"location":"tutorials/sphere-constraint/#use-it-as-a-guard","title":"Use It as a Guard","text":"<pre><code>const char *classify(const Vec3 &amp;p) {\n  using namespace ptn;\n\n  return match(p) | on(\n    $(has&lt;&amp;Vec3::x, &amp;Vec3::y, &amp;Vec3::z&gt;)[inside_unit_sphere] &gt;&gt; \"inside\",\n    _ &gt;&gt; \"outside\"\n  );\n}\n</code></pre>"},{"location":"tutorials/sphere-constraint/#refining-the-constraint","title":"Refining the Constraint","text":"<p>You can combine a named predicate with <code>arg&lt;N&gt;</code> for simple extra checks:</p> <pre><code>const char *classify_upper(const Vec3 &amp;p) {\n  using namespace ptn;\n\n  return match(p) | on(\n    $(has&lt;&amp;Vec3::x, &amp;Vec3::y, &amp;Vec3::z&gt;)[arg&lt;2&gt; &gt;= 0 &amp;&amp; inside_unit_sphere]\n        &gt;&gt; \"inside\",\n    _ &gt;&gt; \"outside\"\n  );\n}\n</code></pre> <p>For multi-value guards, prefer explicit <code>arg&lt;N&gt;</code> references or a callable that accepts all bound values.</p>"}]}