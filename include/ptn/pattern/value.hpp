#pragma once

// Aggregated public API for all value patterns.
//
// This header provides a single entry point to all value pattern factories
// (`lit`, `pred`, `lt`, `eq`, etc.), simplifying user includes.

#include "ptn/pattern/value/literal.hpp"
